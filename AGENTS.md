# Azem Agent Guide

Last verified: 2026-09-01

## Scope

This file applies to the entire repository. It is the working entry point for
maintainers and coding agents: current risks, behavior that must not regress,
verification requirements, and the documentation index all live here.

Only record facts supported by source code, tests, runtime results, or current
GitHub state. New issues require evidence. Update this file when a recorded
state changes; do not use it for temporary task progress.

## Before Starting Work

1. Read this file and the source and tests directly related to the change.
2. Inspect the worktree and preserve existing user changes.
3. Find every caller before changing an exported symbol.
4. Reproduce defects before fixing their root cause. Never hide failures by
   swallowing errors, downgrading the database, or clearing state.
5. Run the narrowest verification that covers the changed path, then the
   relevant complete check before delivery.
6. Update the indexed documentation when behavior, configuration, persistence,
   external dependencies, or operating procedures change.

## Sources of Truth

| Topic | Authoritative source |
|---|---|
| Build commands | `Makefile`, `runtime-js/package.json`, `gpui/Cargo.toml` |
| Go dependencies and version | `go.mod`, `go.sum` |
| Runtime configuration | `internal/config/`, README Configuration section |
| SQLite runtime migrations | `internal/store/sqlite/migrations.go` |
| SQLC compile-time schema | `internal/store/sqlite/dbgen/schema.sql` |
| Agent and scheduler runtime | `internal/agent/`, `internal/app/`, Venat API |
| Sessions and durable timeline | `internal/session/`, `internal/app/tool_timeline.go` |
| Desktop bridge | `internal/desktop/`, `internal/desktop/termhost/`, `internal/desktopipc/`, `internal/daemon/`, `gpui/` |
| Native GPUI and desktop IPC | `gpui/`, `internal/desktopipc/`, `internal/daemon/`, `cmd/azem-daemon/` |
| GitHub PR capability | `internal/githubpr/`, `gpui/crates/azem-gpui/src/surfaces.rs` |
| Native security scanning | `internal/securityscan/`, `docs/security-scanning.md` |
| TUI | `internal/tui/` |
| Executable prompts | `internal/app/prompts/`, `internal/agent/prompts/`, `internal/config/prompts/` |
| User entry documentation | `README.md` |
| Architecture constraints | `.sentrux/rules.toml` |

`internal/**/prompts/*.md` and `internal/skills/bundled/**/SKILL.md` are runtime
inputs, not ordinary documentation. Changing them changes product behavior and
requires matching tests.

## Known Issues and Regression Record

Status values:

- **Unresolved**: users or maintainers are still affected.
- **Fixed, guarded**: the root cause is fixed; the invariant and regression
  coverage must remain.
- **Documentation gap**: implementation exists but independent maintenance
  documentation is incomplete.

| ID | Status | Issue and impact | Evidence | Required handling |
|---|---|---|---|---|
| DB-001 | Fixed, guarded | A user database reached schema 18 while source only supported 17, causing packaged startup to fail with `database schema 18 is newer than supported schema 17`. | `internal/store/sqlite/migrations.go`, `migrations_test.go`, real schema 18 reopen verification. | Keep `schemaVersion == len(migrations)`. Preserve migration 18 control-plane stores, schema 19 project ownership, schema 20 semantic context stores, schema 21 blob offload, schema 22 llmux provider models, schema 23 security scans, schema 24 session graphs, schema 25 auth-broker state, schema 26 webhook receipts, schema 27 Venat execution durability/bindings, schema 28 project-catalog visibility and stale-child repair, upgrade/reopen coverage, and rejection of unknown future schemas. |
| DB-002 | Fixed, guarded | Runtime migrations and the SQLC schema are separate definitions that must remain synchronized. | `internal/store/sqlite/migrations.go`, `internal/store/sqlite/dbgen/schema.sql`, `docs/persistence.md`, `docs/decisions/0001-schema-versioning.md`. | Keep both definitions, the persistence guide, and the schema ADR synchronized. Every schema change tests upgrade, reopen, and data retention. |
| PROJECT-001 | Fixed, guarded | The desktop process previously exposed one fallback workspace while session history was global, so direct app launches lost the real project name, branch, and PR context and rendered every session under the wrong project. | Schema 19 project/session ownership tables, schema 28 catalog-visibility state, desktop bootstrap restore test, session catalog test, multi-project Sidebar and app-only removal tests. | Keep project catalog state in SQLite rather than `config.yaml`; preserve one immutable project owner per session; removing a project hides only its catalog row and never deletes files or sessions; reopening makes it visible; direct desktop launch restores the most recently opened visible project; cross-project sessions open with that project's workspace. |
| STREAM-001 | Fixed, guarded | Commentary, reasoning, and final answers were previously merged, hiding progress around tool calls or duplicating final output. | Provider stream, app event, session timeline, and GPUI `TextPhase` state tests. | Preserve `commentary` and `final_answer` end to end; reasoning must not impersonate commentary; final output renders once. Unphased live text uses the same body-prose chrome as a final answer; do not paint a pending blue-dot card while the phase is unresolved. |
| PROVIDER-001 | Fixed, guarded | Azem previously hard-coded ChatGPT and Grok, so other model providers and arbitrary model IDs could not be configured or routed from the desktop. | `internal/provider/llmux`, generic runtime resolution tests, model-provider action/Bridge/store/settings tests. | Keep ChatGPT/Grok subscription IDs reserved; keep llmux API keys out of YAML and events; validate configured models; preserve one retry owner and typed stream mapping. |
| PROVIDER-002 | Fixed, guarded | DeepSeek's Anthropic-compatible usage reports uncached input and cache-read input separately, while llmux v0.2.4 retained the values without marking the cache counter as reported. Real hits therefore rendered as `Not reported`, and context occupancy excluded cached input. Azem also hoisted every late private system message into Anthropic's top-level system field, rewriting the long-conversation prefix on each turn and reducing real cache hits to about 9%. | `normalizeProviderUsage`, `TestDeepSeekStreamReportsInclusiveCacheUsage`, `TestAnthropicConversionKeepsLatePrivateSystemContextInMessageTail`, and live DeepSeek V4 Flash long-conversation verification. | Keep DeepSeek input usage inclusive of cache-read/cache-write counters, mark zero and non-zero cache reads as reported, leave unknown provider cache support unreported, hoist only leading Anthropic system messages, and preserve later trusted host context at its original message-tail position. |
| PROVIDER-003 | Fixed, guarded | The first Cursor AgentService integration minted a random `conversation_id` for every provider call, selected private evidence as the active user action, rewrote stable root order, dropped images/reasoning provenance, and discarded checkpoints, KV writes, token deltas, native tool history, approvals, and Todo completions. Multi-turn requests therefore lost cache/state affinity, could answer the recap instead of the current task, and could not safely resume tool work. The first bounded transport still allowed a 16 MiB frame to expand into millions of protobuf field descriptors or recurse through unbounded nested Values before JSON validation. | Cursor request/state/exec tests including `TestPrivateTailWireOrderStaysStableWhenTurnBecomesHistory`, `TestConversationCacheIsolatesAccountsWithTheSamePromptKey`, `TestBuildRunRequestReplaysNativeExecProviderState`, `TestMCPDefinitionsAndArgumentsUseProtoValue`, protobuf field/byte/depth budget tests, `TestBuildRunRequestPreservesImageOnlyUserAttachment`, `TestCursorThinkingReplaysOnlyFromSameCursorKimiModel`, app native timeline/Team/Todo tests, and live isolated two-turn Cursor verification (`TURN_ONE_OK` → `TURN_TWO_SAW_TURN_ONE`). | Keep one account-scoped runtime Cursor conversation/blob cache keyed by the existing logical prompt-cache key. Rebuild root messages and turns from canonical history, normalize private tails identically before their active/replayed user, preserve only validated checkpoint non-history state, service bounded content-addressed KV get/set, and rotate a poisoned wire ID once. Main, Team roles, and subagents bind request-scoped governed exec hosts; native calls retain approval, durable timeline, provider-state replay, file observations, and Todo mirroring. Bound transport bytes, decoded protobuf fields, aggregate decoded bytes, and nested Value depth before allocation/recursion. Preserve validated images and same-model Cursor K3 reasoning. Cursor output/context signals must not fabricate billable input or cache hits; unsupported cache-read telemetry remains explicitly unreported. |
| PROVIDER-004 | Fixed, guarded | Restarting with a raw Grok OAuth cache removed the reasoning ladder until a manual refresh, the two-row live response hid seven chat-capable models present in OMP, and Cursor displayed `1M` while budgeting every discovered model at 200k. Cursor's reasoning slider also changed an ignored string instead of the tiered wire model ID, and `max_mode` was dropped. A later family-first picker folded 204 exact Cursor IDs into 33 rows without exposing variant counts and left `(NO ZDR)` in the primary name, making valid variants look missing and an important retention exception unclear. | Real `model_catalog` rows, account/OMP catalog comparison, Cursor privacy documentation, `TestCachedReturnsPersistedCatalogWithoutNetwork`, `TestListModelProvidersAttachesCachedGrokModels`, `TestDecodeUsableModelsParsesOfficialTierContextAndMaxMode`, Cursor family inventory/retention and route-selector tests. | Keep the nine-model Grok curated overlay, non-chat filtering, and provider normalization on both fetch and cached load. Preserve Cursor transport version 3, 1M signal inference, native-family limits, both `max_mode` request fields, and exact raw IDs. Composer and route pickers show one base family. Thinking is implicit and defaults to an exact same-tier variant when available; expose only depth and optional Fast controls, and use standard IDs only for families without Thinking variants. Family rows report enabled variant inventory and retain raw IDs as search aliases. The account's `GetUsableModels` response is authoritative; do not inject absent static OMP rows. Replace raw `NO ZDR` naming with a localized data-retention warning. Unknown Cursor rows remain 200k. |
| PROVIDER-005 | Fixed, guarded | OpenRouter `stealth/ox-alpha` Responses streaming emitted one logical `coding.read_file` twice: a provisional `fc_tmp_*` call when arguments finished and a final UUID `call_id` when the output item finished. llmux v0.2.4 deduplicated by the mutable ID, so Azem executed both. Session `session_7b12ff7c8e94cfa17e27a993`, run `run_1b96da02ec53174e027b67ba` contained 44 duplicate argument groups / 88 records across anchors 120–168 and repeatedly read `docs/desktop.md:366-380`. | Exact `session_tool_records` IDs, arguments, anchors, and nanosecond timings; llmux v0.2.4 `responsesStream.tool` / `updateTool` / `emitTool`; llmux PR [#1](https://github.com/Viking602/llmux/pull/1), release [v0.2.5](https://github.com/Viking602/llmux/releases/tag/v0.2.5), and its cross-protocol identity/security regression matrix. | Keep Azem on llmux v0.2.5 or newer. llmux owns output-index/item-ID/call-ID alias correlation, at-most-once finalization, terminal guards, and count/byte limits across every streaming protocol. Azem must forward canonical SDK tool calls directly; never restore a local `fc_tmp_*` name/argument deduplicator or deduplicate across provider streams/model turns. |
| PROVIDER-006 | Fixed, guarded | Authenticated Cursor `GetUsableModels` failures and empty/malformed responses were converted into successful bundled static rows, which could be persisted as fresh and offered for routing even though the account never advertised them. | `TestFetchUsableModelsRejectsEmptyCatalog`, authenticated catalog error paths, and deep security/architecture review. | Propagate Cursor catalog authentication/network/decode/empty errors. Use only the last successfully authenticated cache through the catalog service's explicit stale path; never persist or route bundled rows as an authenticated account response. |
| PROVIDER-007 | Fixed, guarded | Settings 「从 API 获取」 called the same persistent update path as 「保存提供商」, so probing a temporary endpoint/key silently stored the credential, model catalog, YAML, and runtime config even when the user closed Settings without saving. | `discoverModelProvider`, `updateModelProvider`, Settings credential-consent copy, and deep security review. | Provider discovery is read-only and emits transient model rows to the current UI. Only explicit `set_model_provider` may persist credentials, catalog rows, YAML, or runtime configuration. Never include the pending secret in events. |
| CONTEXT-001 | Fixed, guarded | Rolling summaries mixed durable task state with short-lived transcript evidence and drifted across repeated compactions. | `internal/contextarchive`, archive manifest tests, repeated archive expansion tests, and Inspector projection tests. | Keep one deterministic host archive kernel across automatic/manual/rebuild/Main/Team/subagent paths. Preserve the latest three complete shared user turns, stable source hashes, atomic tool groups, one transactional checkpoint activation, and explicit hard-budget failure. Never restore a model-written rolling summary. |
| CONTEXT-002 | Fixed, guarded | Long subagent reviews spent millions of cumulative input tokens and then failed when a valid model-written semantic checkpoint exceeded its output ceiling; private checkpoint text could also leak into the visible projection or resume seed. | Historical failed `subagent_runs`, archive carrier projection tests, and subagent deterministic-archive tests. | Do not create model-written semantic checkpoints. Keep archive carriers private, persist the complete source as a session-scoped artifact, and never project or resume-seed the carrier as assistant prose. |
| CONTEXT-003 | Fixed, guarded | Automatic compaction failed healthy runs on stale semantic revision/CAS conflicts after concurrent background prepares. | Historical desktop Provider error `semantic state source is stale`, `SaveRunCheckpoint` source-high-water tests, and repeated deterministic archive tests. | Keep exactly one synchronous deterministic archive activation guarded by the run checkpoint source high-water. There is no semantic revision or background prepare to reconcile. A stale checkpoint must rebuild from durable canonical history rather than overwrite it. |
| CONTEXT-004 | Fixed, guarded | Semantic writers often wrapped JSON in Markdown fences, so host validation failed after retries. | Historical desktop compact-history failures and current no-compaction-provider route/config tests. | Context archiving must not parse model output. Raw/fenced JSON handling remains only where another feature explicitly owns that wire contract, such as approvals. |
| CONTEXT-005 | Fixed, guarded | Semantic writers emitted incompatible evidence-source shapes, making otherwise healthy compaction fail closed. | Historical desktop failure after 97 tools / 12m17s and current archive source/manifest validation tests. | Context archiving must derive source refs and exclusions from canonical host records, never from model-generated evidence objects. |
| CONTEXT-006 | Fixed, guarded | A long main run appeared frozen after visible tools completed because hard context maintenance waited indefinitely for a silent compaction provider stream. | Desktop run `run_b6b197f642479f6a19925439`, no-compaction-provider config/runtime tests, and deterministic hard-archive tests. | Context archiving must never open a provider stream or wait on a compaction model. Trigger, reduce, persist, and activate locally; local persistence, digest, or hard-budget failures remain explicit. |
| CONTEXT-007 | Fixed, guarded | SemanticStateV1 made every hard/manual rebuild depend on a model returning valid bounded JSON. Production history had more compaction provider requests than semantic commits, with length, silence, shape, budget, and stale-revision failures. | `internal/contextarchive`, `TestArchiveCompactionKeepsLatestThreeCompleteTurnsAndExpandsOnRepeat`, `TestArchiveCompactionRelaxesOptionalTokenReserveButKeepsLatestThreeTurns`, `TestArchiveCompactionRejectsLatestThreeTurnsThatExceedHardLimit`, `TestArchiveFramesRepairFromDurableSourceAfterRestart`, Team/subagent archive tests, context profile/UI tests, and `docs/context-rebuild-plan.md`. | Keep automatic/manual/rebuild/Main/Team/subagent compaction on the one deterministic host kernel. Preserve exact session-scoped source artifacts, SHA validation, the latest-three-shared-turn boundary, atomic tool groups, bounded optional bitmap carrier, text/artifact fallback, repeated expansion, frame repair, checkpoint CAS, `ArchiveContextManifestV1` policy v3, and `ModelHistory` wire v3. Never restore `agents.compaction`, SemanticStateV1 activation, or a background summary path. |
| CONTEXT-008 | Fixed, guarded | Manifest-free pruning saved the current user turn to the canonical transcript but activated its checkpoint against the pre-turn high-water, so healthy long runs failed with `run checkpoint source is stale: canonical transcript changed while checkpoint was prepared`. A first repair still inferred the boundary from provider-facing message metadata, which is not a durable host protocol and reproduced in the same live session. | Desktop session `session_1eb44415c423640d3d3602d4`, runs `run_ad402930dfd32db5196614d5`, `run_83eae2f02906ee2758c28a81`, and `run_db6ada5b7ccd0f759260c8f7`; `TestCompactionUsesDurableRunUserBoundaryWithoutMessageMetadata`; `TestMainTurnPrunedCheckpointCoversCurrentUser`; and the complete `internal/app` test suite. | Persist the current user block before building provider history. Automatic and resumed activation must take the checkpoint CAS boundary directly from that run-owned durable user block, never from `ProviderState` or provider-facing message metadata. Manual compaction uses the durable projection high-water. A canonical message appended after preparation must still fail the checkpoint CAS and rebuild from durable history. |
| CONTEXT-009 | Fixed, guarded | Manual compaction failed on legacy sessions when terminal `session_tool_records` survived but an older ModelHistory had already removed the matching raw tool call/result pair before deterministic archive sources existed. | Desktop session `session_1eb44415c423640d3d3602d4`, tool call `call-730d961e-2dbf-49cd-896b-8d065f0c92b8-350`, 102 missing legacy pairs including three artifact-backed results, and `TestManualCompactionRestoresDurableToolPairMissingFromLegacyHistory`. | Before manual Snapcompact archiving, restore a call/result pair from its durable ToolRecord only when both sides are absent. Preserve anchor ordering, failed state, deterministic timestamps, full artifact-backed results, and complete-turn validity. Partial pairs, name mismatches, missing artifacts, and invalid records remain explicit failures. |
| CONTEXT-010 | Fixed, guarded | A conversation reached 98% provider-reported context occupancy without automatic archiving because the loop compared a lower local `bytes/4` estimate against a trigger that reserved only 16,384 tokens. In the reproduced Grok 4.6 run, provider input was 489,188/500,000 while the local estimate remained 476,653 below the old 479,931 trigger. | Desktop session `session_1eb44415c423640d3d3602d4`, run `run_9555fdf14323129a5e3a626e`, generation 17 archive evidence, `TestContextBudgetTriggersBeforeReproducedGrok98PercentOccupancy`, `TestArchiveCompactionUsesProviderReportedPressureWhenLocalEstimateIsLow`, and metered-driver callback coverage. | Keep the effective reserve at `max(reserve_tokens, 15% of context_window)`. Automatic Main, Team, and subagent decisions use the larger of the local history estimate and the latest completed provider input minus tool definitions. Reset provider pressure only after successful archive activation. Do not change the deterministic reducer target, static prompt, or provider message order. |
| VERIF-001 | Fixed, guarded | A Plan-UI rewrite still had `in_progress`/`pending` Todo items, but `current-work-verification` replaced or appended to the model output after one retry with `Verification evidence is missing or stale...` and ended the run. The canned host verdict was persisted as `RoleAssistant`, so the desktop presented it as if the model had written it. A later repair removed the host text but mapped `uncertain`/`fail` to `AllowOutput`, making an unverified model claim look successfully completed. | Desktop session `session_13f738b36beebc3454259f9c`, run `run_8e030f82b2b8682c52824e0a`, `TestIncompleteTodoItemsKeepsOpenWork`, surface-verification block tests, and the legacy Timeline filtering test. | Any `pending`/`in_progress` Todo blocks a model-initiated finish, including read-only parent turns. Retry until those items are completed or cancelled; user stop still cancels. Persist uncertain/failed verification state internally and fail the terminal run with a host-owned guardrail reason after the allowed retry. Never inject host wording into assistant output or silently allow an unverified completion. Strip the two exact legacy notice suffixes when rendering old durable blocks. |
| VERIF-002 | Fixed, guarded | `current-work-verification` generated a `gofmt -d <files>` check but recognized only an identical `coding.shell` command; completed `coding.gofmt` calls with current post-format SHA evidence were ignored. The model produced a final answer after formatting, Go tests, readback, and `git diff --check`, then the guard injected a retry, so the UI appeared finished and unexpectedly resumed work. A separate no-op `coding.gofmt` (`changed=false`) was also treated as a fresh mutation and could invalidate earlier checks. | Desktop session `session_3c7d5fadfa89577de5f95be2`, run `run_0d2931a0ec3e0fe2fa2620b7`, verification plan `verification:a39d685e56e7ed09`, `TestDedicatedGofmtSatisfiesFormatterVerification`, and `TestUnchangedGofmtDoesNotAdvanceMutationBoundary`. | Accept completed dedicated `coding.gofmt` records as formatter evidence only when every required path's recorded SHA equals the current work revision. A formatter result with `changed=false` is observation, not mutation, and must not advance the mutation boundary. Retry after a model final answer only for genuinely missing/stale evidence. |
| RUNTIME-001 | Fixed, guarded | Venat upgrades changed Usage, control-plane, and execution durability contracts; retaining v0.15 Runner/store adapters across the v0.16 execution boundary would split ownership and make restart behavior ambiguous. | `go.mod`, `internal/agent`, `internal/agentruntime`, schema-27 durable backend/binding tests, upstream Venat v0.16.1 contract suites. | Keep a clean v0.16.1 cutover: one service-lifetime `durable.Runtime`, direct `agent.Engine` composition, immutable v1 execution bindings, and no legacy Venat imports or compatibility aliases. Azem owns application policy/orchestration/projection; Venat owns one execution lease/checkpoint/effect attempt/receipt. Verify the real module with `GOWORK=off` and the pinned upstream contract surface. |
| RUNTIME-002 | Fixed, guarded | Opening another project window previously ran global crash recovery against the shared SQLite database, expired leases owned by a still-live Azem process, and made the original run fail with `stale task version` or become `reconcile_required`. A second main task also surfaced a transient workspace claim conflict as a terminal provider failure. Clean cold starts later repeated the full durable recovery scan even though the preceding daemon had shut down normally. | `internal/store/sqlite/recovery_fence*`, recovery-fence tests, `TestMainRunWaitsForWorkspaceClaimInsteadOfFailing`, and durable run/tool/subagent evidence from `run_ee2cd85db941608e69b4de4b`. | Acquire the process fence before legacy relocation or SQLite open. Only its exclusive owner may migrate, prepare legacy recovery, construct/classify durable execution recovery, and publish the boundary. Hold the shared fence until agents and persistence close. Only the last successful shutdown publishes the clean marker; clean cold starts skip crash recovery, while crashes and incomplete shutdowns retain the recovery path. Session navigation must not emit `SessionEnd`; transient resource-claim conflicts wait/retry instead of persisting raw failures. |
| RUNTIME-003 | Fixed, guarded | Main and shared-workspace writer tasks took one exclusive `azem:workspace-write:` claim per project. A live or `reconcile_required` holder made every later session wait before any provider request, so conversations looked offline. | Desktop sessions `session_53368f9884e9728a5af01054` and `session_b0aeb5be0a0fb650a78cbee4` waiting behind `claim-590`; `topLevelWorkspaceWriteClaims`, `subagentResourceClaims`, and `TestPrepareRecoveryExpiresOrphanWorkspaceClaims`. | Do not attach exclusive workspace-write claims to main sessions or subagents. Crash recovery expires leftover active claims. Keep the recovery fence and wait/retry path for any other transient claim conflict. |
| CONCURRENCY-001 | Fixed, guarded | Agent definitions omitted parallel tool mode, so Venat serialized whole batches. Foreground subagent spawns and shell calls appeared queued even when their runtime limits had free capacity. | Main/Team/Subagent/automation `agent.Engine.ToolMode`, engine behavior tests, Venat parallel dispatch tests. | Keep every direct main, Team-role, Subagent, and automation engine in parallel tool mode. Preserve separate shell/Subagent concurrency limits and sequential Skill activation when later calls depend on newly registered tools. |
| SUBAGENT-001 | Fixed, guarded | Foreground subagents were cancelled when a ten-minute parent wait window elapsed or the parent tool context ended, so valid long investigations could not finish despite unbounded token, turn, and wall-clock budgets. | `subagentSpawnDriver`, `continueInBackground`, long-running lifecycle tests. | A wait window of `0`/`0s` waits until the foreground child completes. A positive window releases only the parent call. Safe work continues durably in the background; unsafe shared-workspace writes keep waiting. Cancel children through explicit kill, an explicit include-children stop, application shutdown, or an optional configured `idle_timeout` (SUBAGENT-005). |
| SUBAGENT-002 | Fixed, guarded | Subagent concurrency was fixed to a small positive value and recursive delegation was hard-disabled, unlike OMP's scalable task runtime. A naive recursive queue can also deadlock when a waiting parent holds the only slot. | Recursive-depth tool exposure, unbounded-concurrency update, advisory-budget, and concurrency-one recursive completion tests. | Keep default concurrency 32 with zero meaning unbounded; keep recursive depth 2 with `-1` unlimited and `0` disabled. Permit only one re-entrant child per waiting parent beyond a full limit. Soft request budgets may advise but must never cancel work. |
| SUBAGENT-003 | Fixed, guarded | The default foreground wait was a positive duration (`10m`), so a still-running foreground child released the parent tool and, when safe, continued in the background. Users who wanted the parent to wait while work stayed foreground had to pick a finite cap; `0` and `-1` were rejected. | Default `await_timeout: 0s`, `waitForForeground`, `TestForegroundWaitUntilCompleteKeepsParentOnZeroWindow`, Settings 「直到完成」. | Keep `0`/`0s` as wait-until-the-foreground-child-completes. A positive window still only releases the parent (SUBAGENT-001). Do not treat `-1` as a second unlimited sentinel. Do not cancel children when a finite window ends. |
| SUBAGENT-004 | Fixed, guarded | The main prompt allowed review and verification to run in the background, and the runtime never checked for still-running children when the parent produced a final answer. Slow review children therefore finished after the parent had already moved on; later auto-wake turns only injected raw English user text. | `pending-background-children` OutputGuardrail, `TestPendingBackgroundChildrenGuardrailKeepsRetryingUntilTerminal`, `TestBackgroundCompletionsBatchIntoOneWake`, `TestMarkParentChildrenDeliveredSuppressesAutoWake`, and the main prompt contract. | A parent must not finish while any child of the current run is still non-terminal. The guardrail keeps retrying until every child is completed, failed, or cancelled. Do not restore a one-shot retry or an independence claim. The host may cancel a silent child after `idle_timeout` (SUBAGENT-005). After a successful parent finish, mark that run's terminal children delivered so auto-wake does not start a second stream. Batch remaining undelivered completions only when the parent did not complete successfully. |
| SUBAGENT-005 | Fixed, guarded | After a tool completed, a long architecture-review child could stay `运行中` for 20+ minutes with no thinking or output because the next provider stream has no idle watchdog (streaming HTTP uses zero timeout). The first watchdog defaulted to `0s` (disabled), so silent stalls lasted until the user cancelled. Live thinking could also exist while the card/drawer showed only 运行中: `inspect_agent` replaced newer deltas, and projection resync reloaded only the main session. | Desktop silent-stall screenshot, `TestIdleTimeoutCancelsSilentRunningSubagent`, `TestIdleTimeoutSkipsOpenToolAndResetsOnThinking`, `TestIdleTimeoutResetsOnTextAndToolAndIgnoresEmptyThinking`, `TestIdleTimeoutSkipsLiveShell`, Settings 「无响应自动取消」, side-chat thinking tests. | Keep `idle_timeout` default `5m`. Zero still disables. A positive window cancels only a running child with no thinking, text, or tool activity, no open tool, and no live `coding.shell`. Empty thinking/text frames and elapsed UI ticks are not activity. Compaction and explicit wait summaries reset the clock. Running cards/drawers must show live thinking or the wait pill, not a bare 运行中 body. A same-session `session_loaded` with `state=refreshed` must keep `selectedAgentId` and `agentBlocks` so the follow-up `inspect_agent` can merge. |
| SUBAGENT-006 | Fixed, guarded | Desktop stop called `CancelActive(false)`, so a user-stopped parent detached still-running children into the background. An architecture-review child stayed `运行中` after the stop button. | Desktop collaboration roster after stop, `TestUserStopCancelsRunningSubagents`, ThreadSurface stop-with-children test. | Desktop stop is an explicit include-children stop. Parent-wait expiry and TUI “parent only” still must not cancel children (SUBAGENT-001). |
| SUBAGENT-007 | Fixed, guarded | The subagent runtime had three blocking amplifiers: `Cancel` held the global runtime lock while writing SQLite, so a slow store stalled frame handling and scheduling for every child; the lease/workspace 100 ms retry loop refreshed `lastVisibleAt` and re-saved the run on every spin, so a purely waiting child never triggered the idle watchdog and hammered the store; and live `agent_state` events were not coalescible, so streaming children flooded the event queue. | `TestIdleTimeoutStillFiresDuringRepeatedWaitHeartbeat`, `TestEventBrokerReplacesLiveAgentStateByStreamIdentity`, `TestEventBrokerKeepsDistinctAgentStateTransitionsOrdered`, `TestEventBrokerKeepsTerminalAgentStatesOrdered`. | Never hold `r.mu` across store writes in Cancel. A new explicit wait summary still resets the idle clock once (SUBAGENT-005), but repeating the identical heartbeat must not refresh `lastVisibleAt`, write the store, or emit state. The broker collapses only same-state live `agent_state` snapshots per stream identity; distinct lifecycle transitions and terminal states stay ordered and are never dropped. |
| SUBAGENT-008 | Fixed, guarded | A foreground read-only child inherited the parent session's unfinished Todo through `newMutatingVerificationGuardrail`. It found the requested sentinel on its first tool call, but every valid final answer was rejected with `This run still has unfinished Todo items`; the child made 26 provider attempts (25 completed, one interrupted/unknown) while the UI emitted repeated `running` snapshots until the live validation was stopped after 18m45s. | OpenRouter-only failed parent `run_bf9371f84b8b058c568a9c20` / child `run_e5beac32820eb7cbbe731c9e`; corrected parent `run_85a13a57508616ce73658292` / child `run_0e0ff4837f3df2c97a6eeaf0`; and `TestSessionTodoGuardDoesNotBlockSubagentCompletion`. | Enforce the durable session Todo only on the parent/main output guardrail. A child still runs its own mutation/evidence verification, but it must not inherit the parent item's completion gate. The parent remains blocked by `pending-background-children` until the child becomes terminal. |
| SUBAGENT-009 | Fixed, guarded | During shutdown, a child startup or heartbeat could clone `running`, then cancellation persisted `cancelling`, and the older unlocked save could overwrite SQLite back to `running`. Cancellation also reused the already-cancelled runtime context, turning shutdown into `persist cancelling subagent: context canceled`. Real parent run `run_ec86dfb719b945c0f6cb38b5` left five `running`, one `queued`, and four falsely failed child rows. | Live `subagent_runs` and durable binding evidence; `TestSubagentCancellationWinsOverInFlightRunningPersistence`; `TestSubagentCancelPersistsAfterRuntimeContextCancellation`; `TestMigrationV28AddsProjectVisibilityAndRepairsIncompleteSubagents`. | Serialize durable writes per child without holding the global runtime lock. Cancellation uses a bounded lifecycle-independent context and must be the final non-terminal write; terminal persistence must follow every earlier write. A stale `running`/`queued` snapshot must never resurrect a cancelling or terminal child. Schema 28 interrupts rows stranded by older binaries. |
| TERM-001 | Fixed, guarded | The embedded terminal could hang the Bridge and the window exit path: `shutdown()` waited on `done` without bound while `cmd.Wait()` can stall on an uninterruptible child, `CloseAll` shut sessions down serially, PTY writes blocked forever when the shell stopped reading, `Create` held the host lock across fork/exec and could leak a session past a racing `CloseAll`, and the renderer rerendered every tab per output chunk with an unbounded backlog and unordered parallel writes. | `TestShutdownReturnsWithinBudgetWhenSessionNeverConverges`, `TestCloseForgetsSessionThatNeverConverges`, `TestWriteTimesOutInsteadOfHangingWhenShellStopsReading`, `TestCloseAllRacingCreateLeavesNoRunningSession`, GPUI terminal state/surface tests. | Every termhost wait stays bounded: close budgets, background bounded reap, parallel `CloseAll` under one overall budget, and non-converged sessions leave the roster with a log line. Writes fail with `ErrWriteTimeout`/`ErrWriteBusy` instead of blocking; fork/exec happens outside the host lock with a closed re-check. The renderer keeps `terminal_output` out of session metadata, frame-batches terminal updates with a bounded backlog, serializes one timed desktop write per session, skips unchanged resizes, and removes a closed tab before the backend close converges. |
| TOOL-001 | Fixed, guarded | Queued tools were once rendered as running, making approval wait time appear as execution time and leaving later calls spinning. Idle announcements later reused that queued label even when no sibling was executing. Auto-review batches later sat in 排队中 instead of reviewing together. | `provider_execution.go`, `provider_approval.go`, GPUI state/surface tests, lifecycle tests. | Preserve `queued -> awaiting_approval -> running -> completed/failed` for permission waits. Auto-review announcements emit `reviewing_approval` and start reviews in parallel. Pending review shows a shield and 审核中, never 排队中. Do not show unexecuted writes as file changes; converge every non-terminal tool when a run ends. |
| TOOL-002 | Fixed, guarded | The desktop stop action first synchronously waited for Venat to finish cancelling the active durable run before returning through the Bridge, so slow MCP/tool cleanup left the stop button visibly stuck. Moving the whole call into a goroutine then introduced a delivery race: a provider could finish before the goroutine signalled Venat, persisting `failed` instead of `cancelled`. | `CancelActiveWithChildren`, `TestCancelActiveReturnsBeforeUncooperativeExecutionFinishes`, 100 repeated non-race runs, 20 repeated race-detector runs, and real desktop stop verification. | Signal the active durable coordinator synchronously before returning through the Bridge, but wait for durable cleanup in the bounded background path. Keep the coordinator as owner of the terminal cancellation cause, then cancel the app-owned run context after cleanup converges. |
| TOOL-003 | Fixed, guarded | The first repeated-read fix assumed the calls were model-authored and changed only the core prompt to reuse completed reads. That policy gap was real, but the same visible symptom remained because the investigation had not compared durable call IDs: OpenRouter was emitting one logical call under both provisional and canonical IDs (PROVIDER-005). | First and second desktop repeated-read screenshots, `TestMainInstructionsContract`, and later session/run evidence showing paired `fc_tmp_*` / UUID calls with identical arguments. | Keep the read-reuse/hashline guidance, but never rely on prompt text to correct duplicate provider wire events. The released llmux parser owns correlation and at-most-once emission under PROVIDER-005. |
| TOOL-004 | Fixed, guarded | The POSIX shell detector first treated descriptor syntax such as `2>&1` as detached execution. A later repair allowed real `&` when a wall clock was supplied, but process-group cleanup can be escaped by a new session; quote concatenation such as `set''sid ... &` bypassed exact-token checks and could leave a child alive after normal return, timeout, cancellation, or shutdown. | `TestShellFDDuplicationRedirectionIsForeground`, `TestShellRejectsBackgroundOperator`, `TestShellRejectsBoundedBackground`, `TestShellRejectsDetachedForms`, and deep security review. | Keep descriptor/pipeline forms distinct from background syntax, but reject every POSIX background operator. Accept foreground commands only, bound them by inactivity/wall-clock limits, reap the owned process group, and normalize shell quote/backslash fragments before rejecting known detachment primitives as defense in depth. |
| TOOL-005 | Fixed, guarded | `coding.replace` could rewrite a file from the 1 MiB truncated `coding.read_file` prefix, return the inner Hashline call identity, and lose trailing empty lines. Failed Go tests and replace/delete observations also had inconsistent status/projection semantics. The first delete implementation additionally checked only lexical containment, so `workspace/link -> /outside` plus `link/file` could remove a regular file outside the workspace. | Replace/Go-test/file-observation regressions, `TestDeleteFileRejectsSymlinkedParentEscape`, and deep security review. | Preserve outer tool identity and exact bytes; reject whole-file replacement from truncated reads; align command/tool error state and durable observations. On supported Unix platforms, delete through root-anchored `openat`/`unlinkat` traversal with `O_NOFOLLOW` on every parent and `AT_SYMLINK_NOFOLLOW` on the leaf. Unsupported platforms fail closed rather than using raceable lexical deletion. |
| TOOL-006 | Fixed, guarded | A live OpenRouter `stealth/ox-alpha` response produced one `coding.write_file` argument object with two `path` keys (`created.txt` and `delete_me.txt`). Standard JSON decoding silently selected one value, so the wrong target reached the driver and the model had to repair the batch. | Isolated session `default`, run `run_19dbda534e3c817e1eb4d13d`, canonical provider state for call `3f034f09-4744-43e0-8e1b-fc6dde80a153`, `TestDuplicateToolArgumentKeysFailBeforeApprovalOrExecution`, and `TestToolArgumentValidationRejectsNestedDuplicateKeys`. | Validate every governed tool's raw argument object before approval or execution. Reject duplicate keys at any nesting level, including equivalent escaped keys, as a recoverable tool error with the original call identity and no side effect. Do not rely on ordinary last-key-wins JSON decoding. |
| TOOL-007 | Fixed, guarded | `coding.search` delegated to Venat's default `ListFiles` walker, which stopped after the first 1,000 eligible files and ignored only `.git`; a first repair still enumerated every candidate with Git and scanned file contents in Go. In the real Azem workspace, generated/dependency files placed `internal/app/work_evidence.go` at eligible position 15,921, so the original search returned no matches with `truncated=true` and the model abandoned the tool. | User screenshots/commentary, live workspace census, `TestSearchSkipsIgnoredTreesInsteadOfTruncatingBeforeSource`, `TestReliableSearchUsesRipgrepRegexPathGlobAndEmptyResult`, and packaged ripgrep verification. | Workspace search uses one bundled ripgrep process with argv-only `--json`, literal-by-default matching, ripgrep regexp/glob syntax, ignore handling, a 1 MiB file ceiling, and cancellation at the global 200-line limit. Sort only the bounded matched paths, then route each through `coding.read_file` so Hashline anchors remain editable. Do not restore a workspace walker, per-candidate Go scan, or system-ripgrep runtime dependency. Missing/corrupt bundled ripgrep fails explicitly. Exact internal resource URIs remain in-process. |
| TOOL-008 | Fixed, guarded | Desktop stop addressed only the process-local `activeEnd`, so a renderer targeting a durable pending or suspended run received `cancelled=false`; the GPUI request was not tracked, making the stop button appear inert and leaving pending controls visible. | User report, durable binding evidence, `TestCancelRunByIDCancelsPersistedRunForOwningSession`, `TestCancelRunWithChildrenCancelsPersistedRun`, and GPUI pending-question/terminal-control tests. | Carry the exact session and run through `cancel_active`. Keep active cancellation on the synchronous coordinator-signal path, cancel persisted pending/suspended bindings only for their owning session, resolve pending question blocks, clear terminal controls, and show stopping/error feedback until a terminal event converges. |
| APPROVAL-001 | Fixed, guarded | Configurable Anthropic-compatible approval models can ignore native response-schema options and return the decision as a Markdown JSON fence, which previously caused `Automatic review failed (parse)` and prevented an authorized action from running. | `internal/provider/codex/guardian_policy.go`, `reviewer.go`, `guardian_outcome.go`, provider reviewer and app automatic-approval tests. | Keep the explicit JSON-only output contract and strict fail-closed validation. Accept only raw JSON or one whole-response JSON fence; never extract a decision from surrounding prose or execute after an invalid review. Apply the Codex host matrix so `low`/`medium` denials do not fall back to a person unless the rationale is an explicit injection or tenant deny. |
| SKILL-001 | Fixed, guarded | Skill activation is run-scoped Venat memory, while durable transcripts keep the prior `hydaelyn_activate_skill` result. After a failed compact or a later turn, the model treated `check` as already active and called `hydaelyn_read_skill_resource`, failing immediately with `skill "check" is not active`. | Desktop retry 0s failures, `loadSessionActivatedSkills`, `TestProviderRuntimeReplaysActivatedSkillsOnLaterTurn`, and `TestProviderRuntimeDoesNotReplayDisabledOrDeletedSkills`. | Replay completed activations from session tool records into the next run's Skills set. Drop names that are no longer resolvable. A session that never activated the skill still fails closed. |
| SKILL-002 | Fixed, guarded | Replaying a session-activated Skill used to change the next run's static Skill set and activation/resource schemas, invalidating the provider prefix and starting a cold cache epoch. | Production session `session_1eb44415c423640d3d3602d4`; `TestSkillActivationAuthorizationRestoresWithoutChangingProviderPrefix`; Azem replay tests. | Restore resource-read authorization from durable activation records without adding eager Skill bodies or changing the advertised Skill tools. The system prefix and tool definitions must remain byte-stable across activation and replay. |
| SKILL-003 | Fixed, guarded | A child that called `hydaelyn_read_skill_resource` on `SKILL.md` (or an inactive skill) received a Venat driver Go error, so the whole subagent loop died and the desktop showed `missing tool result`. | Desktop architecture-review child, Venat `skillResourceDriver.Execute`, `TestRunContinuesAfterUndeclaredSkillResource`, `TestProviderRuntimeSkillMDResourceDoesNotFailRun`. | Skill activation and resource misses return `IsError` tool results. Do not abort the run. `SKILL.md` is the activation body, not a resource. |
| TODO-001 | Fixed, guarded | Parallel Todo mutations shared stale revisions and could skip/demote work. The model-facing `init` schema later exposed host-owned identity/status, then documented an omitted-`op` compatibility path while still marking `op` required, so Venat rejected the payload before the driver could infer `init`. | Todo concurrency/identity tests, `TestTodoDriverInfersInitFromUnambiguousPayload`, `TestTodoDefinitionKeepsInitIdentityHostOwned`, corrected live run `run_1e23618b7fc49e2f60463c72`, and deep architecture review. | Keep Todo mutations serial and revisioned. The `init` schema must allow `op` to be omitted only when goal+phases unambiguously define initialization; the driver rejects every other missing operation. IDs/status remain host-owned. `done` is the normal completion transition and automatically advances the next item. |
| UI-001 | Fixed, guarded | Todo, Subagents, active-thinking motion, and process groups once existed only on an integration branch and disappeared from the GUI branch. | Commit `c2e4030`, GPUI state/surfaces and native regression tests. | Move these projections and views as one unit. Do not restore the old `AgentList` or port styling without the store/event projection. |
| EXT-001 | Fixed, guarded | The Extensions surface previously exposed only raw Skills, then loaded imported plugins directly from Codex source/cache paths and described the whole capability as “Codex plugins”; a populated startup catalog could also appear empty when its initial event preceded the renderer listener. | `internal/plugins`, `ActionListPlugins`, desktop `plugin_catalog` origin projection, Extensions UI, plugin copy/local-discovery and snapshot replay tests. | Keep all runtime roots under Azem `plugin-packages`; direct installs use `local`, Codex entries remain catalog-only until explicitly selected in `plugins.codex_imports`, and selected imports use staged copies under `codex`. A Codex outage must not invalidate existing selected copies. Preserve validated image-data projection, manifest validation, existing Skill/MCP/hook boundaries, default-denied Hooks, and explicit OAuth/App degraded states. |
| EXT-002 | Fixed, guarded | The desktop Extensions surface derived MCP totals only from plugin metadata and never projected the live MCP manager, so configured servers disappeared and the page looked empty when no plugin was installed. | `internal/app/mcp_settings.go`, `mcp_state` GPUI projection, native Settings tests, and real `Azem-GPUI.app` lifecycle verification. | Keep MCP independent from plugin discovery. Preserve typed add/enable/reconnect/delete actions, atomic configuration writes, persistent deletion tombstones, secret-free snapshots, asynchronous lifecycle work, explicit empty states, and live manager tool removal when disabled or deleted. Built-in and plugin-owned services are deletable; Codex-only `computer-use` launchers must never enter the Azem MCP runtime. |
| EXT-003 | Fixed, guarded | Clicking 导入 on a visible Codex plugin such as kami left the row unchanged. The settings dialog hid the Bridge error, a missing wire `id` sent an empty target, and catalog discovery could abort the copy even when the local marketplace checkout or cache was present. | Desktop kami row `Codex · kami · 1.12.0`, `TestSetCodexPluginImportedUsesKnownCatalogWhenCodexListFails`, `TestDiscoverImportsFromMarketplaceCheckoutWhenCodexListFails`, and ExtensionsSettings missing-id import test. | Reconstruct `name@marketplace` when `id` is omitted. Surface import failures on the Extensions/settings surface. Read Codex's local config and package directories without launching its CLI. Copy from the current scan, last known catalog, Codex cache, or `.tmp/marketplaces/<marketplace>/plugins/<name>`. |
| EXT-004 | Fixed, guarded | The Hooks trust confirm reused the MCP delete grid with a `<header>`/`<h3>` title, so 「信任插件 Hooks？」 wrapped into three poster-sized lines. Clicks on 信任并启用 could hit the backdrop or miss the squeezed heading, so trust never persisted and the overlay could also block plugin import. | Desktop Extensions Hooks screenshot, `TrustHooksDialog` now matching `DeletePluginDialog`, ExtensionsSettings trust-action/error tests, and SettingsDialog overlay host test. | Keep the confirm as a 420px delete-density dialog with one-line `h2`, warning copy, and a clickable primary button. Host it on the settings `<dialog>`. Emit `set_plugin_hooks_trusted` with `decision: "true"`. Surface save failures. Do not default `plugins.trust_hooks` to true. |
| EXT-005 | Fixed, guarded | Settings → Extensions → Hooks showed 0/0 and 「还没有 Hooks」 after restart until refresh; trusting plugin hooks left the switch off; individual hooks could not be disabled. Desktop `prime()` omitted `list_hooks`, there was no `HookCatalog()` readback, and a late `hook_catalog` was dropped once `lastSequence` moved ahead. | `Bridge.HookCatalog`, prime `ActionListHooks`, `hooks.disabled` / `set_hook_enabled`, `TestBootstrapEmitsHookSnapshotAndDirectReadback`, `TestSetPluginHooksTrustedPersistsAndReloads`, `TestSetHookEnabledPersistsAndDoesNotExecute`, SettingsDialog/ExtensionsSettings/store catalog tests. | Keep catalog replay and sequence-0 readback after subscribe. Persist trust through `plugins.trust_hooks` and reload runtime; never default it true. Keep per-hook deny-list dispatch skip fail-closed: untrusted plugin hooks do not run even when enabled. |
| EXT-006 | Fixed, guarded | The native GPUI Extensions page rendered read-only inventory cards without management actions. It read MCP's JSON-encoded `data.servers` as a top-level list, replaced Hook catalogs with activity events, and selected the first market array even when empty. | `extension_snapshots_survive_activity_and_failed_refreshes`, `extension_actions_preserve_ownership_scope_and_confirmation`, `marketplace_keeps_sources_and_installed_plugins_when_available_is_empty`, and existing backend extension action/persistence tests. | Decode only MCP snapshot events; preserve catalogs on activity and refresh errors. Keep tab-specific search/refresh and existing backend management actions, explicit source/command/market groups, pending/error feedback, exact plugin IDs, user/project scope, Azem-owned import deletion, and native confirmations for destructive actions and Hook trust. Never default plugin Hook trust on. |
| EXT-007 | Fixed, guarded | Native extension toggles inserted a pending row above the lists and dimmed every control during each save, visibly flashing and shifting the page even when the Hook state persisted correctly. Plugin rows omitted both catalog logos and their plug fallback. | Isolated Hook enable/save readback; `extension_pending_feedback_does_not_reflow_or_dim_controls`, `extension_plugin_rows_keep_icons_and_a_plain_heading`, and `extension_logos_only_decode_bounded_self_contained_image_data`. | Keep pending feedback outside layout flow, stable control IDs, serialized actions, and explicit error feedback without fading the entire toolbar. Render bounded self-contained image data with a plug fallback; never resolve plugin logo URLs or filesystem paths. Keep the list heading concise and deletion scope in the confirmation. See `docs/desktop.md`. |
| UI-002 | Fixed, guarded | A slow desktop renderer could exceed the runtime event high-water mark and terminate an otherwise healthy provider run with `UI event backlog exceeded the safe limit`. | `internal/app/event_broker.go`, `event_broker_test.go`, desktop/TUI refresh actions, GPUI replay/state tests. | UI projection pressure must never become a provider error. Keep replaceable deltas coalesced by stream, lifecycle and approval events lossless, durable projection resync after compaction, bounded tool previews, and lightweight live rendering. |
| UI-003 | Fixed, guarded | Switching live assistant output to immediate Markdown removed the per-delta text reveal because existing paragraph nodes no longer remounted; the old plain-text reveal could not be restored without delaying Markdown syntax. Marking every block child and the last eight ranges then replayed fade/blur on already-written final-answer lines each time the source grew. | `StreamingMarkdown`, `liveRevealRanges`, `StreamingText`, Timeline live Markdown/reveal tests, and the no-block-in CSS assertions. | Parse Markdown on every coalesced frame. Wrap at most eight increments younger than 260ms. Resume in-flight motion with a negative `animation-delay` so reparse cannot replay settled siblings. Incoming text is a readable ghost (`opacity: .28`, `blur(5px)`) that sharpens in 260ms. Do not translateY inline spans. Honor both system and explicit reduced-motion settings. |
| UI-004 | Fixed, guarded | A conversation could finish after the user switched away without leaving any visible notification, so the completed result was easy to miss. | Cross-session terminal reducer tests, Sidebar unread-dot test, and `TestMarkSessionUnreadPersistsUntilResume`. | Only a tracked foreign main run that succeeds or fails marks its session unread. Persist the blue dot through `session_ui_state`, clear it on resume, and exclude cancellation and subagent terminal events. |
| UI-005 | Fixed, guarded | Cross-project sidebar and search rows called `launch_gpui_window`, spawning one GPUI process and visible UI per selected conversation instead of switching the existing window. | `project_session_navigation_reuses_the_existing_window`, `workspace_switch_detaches_without_stopping_the_daemon`, and packaged cross-project session switching. | Keep exactly one GPUI window. Cross-project navigation must detach the renderer without stopping the old workspace daemon, reconnect the same window to the target daemon, and restore the owning session. Never spawn another renderer for a project or session click. |
| UI-006 | Fixed, guarded | Active file edits previously used the generic tool disclosure and exposed raw Hashline arguments, then changed shape only after the tool completed. | `pendingFileChangeSummaryForBlock`, the live-to-completed Timeline regression test, and real packaged desktop verification. | Keep queued and approval-bound writes out of file-change projections. Active edits may show exact planned totals only when the arguments are unambiguous, and must transition in place to the completed structured diff. |
| UI-007 | Fixed, guarded | The Subagent conversation drawer previously split one transcript across task, metadata, activity, and content panels, so it no longer read like the main conversation and completed work remained visually expanded. Live process trails could also collapse to a 处理中 summary while work was still running. | `AgentSideChat`, `Timeline.tsx`, the process-only drawer regression in `App.test.tsx`, and packaged desktop verification. | Keep Subagent user prompts and final answers in the same transcript typography as the main conversation. Active commentary and tool trails stay expanded and cannot fold to 处理中. Only completed tool/diff trails fold under 已处理; they start folded and may be reopened. A live run keeps the open commentary and count-row trail. After that turn ends, fold the whole tool trail and keep the final answer in the reading column. Expanding a large fold must not mount every tool body at once. Thinking-only or commentary-only groups keep the live thinking header (UI-016). |
| UI-011 | Fixed, guarded | The desktop search field previously filtered only a small in-memory command/session-title list, so settings and durable conversation content could not be found or opened. | SQLite global search tests and benchmark, native search/settings/navigation tests, sequence-focus tests, and packaged desktop verification. | Keep settings/catalog search local and session content on the bounded SQLite FTS path. Never copy complete transcripts into renderer search state; preserve stale-response rejection, cross-project ownership, stable block-sequence focus, and direct projection readback after resume. |
| UI-012 | Fixed, guarded | Opening a long Subagent drawer while it streamed made the side chat stutter: every subagent delta recopied the main `blocks` array, folded process trails still mounted their children, and header preview updates re-rendered the whole transcript. Expanding a large completed fold later mounted every tool body at once. | `reduceEvents` reference-stability test, folded `ProcessFold` mount test, deferred expand tests, `AgentSideChat` transcript split, and UI-003/UI-007 regressions. | Keep collection identities stable unless that collection changed. Folded process trails must not mount `ProcessEntries`. Expanding a large fold first paints collapsed chip headers and must not mount every diff, Markdown, Thinking panel, or Subagent list at once. Opening a settled count row must not mount the 思考 paragraphs until that chip is opened. Parse Markdown on every coalesced frame, but reuse equal reveal-range identities. Keep the drawer transcript on its own store subscription. |
| UI-013 | Fixed, guarded | Background subagent completion auto-wake persisted a `Kind=user` prompt titled `You`, so the desktop rendered host delivery text as a user bubble and the model treated it as ordinary chat. | `userTurnBlock` `state=subagent_wake`, Timeline wake-notice test, and `TestUserTurnBlockMarksSubagentWake`. | Keep wake blocks as `kind=user` so they remain in model context, but render `state=subagent_wake` as a left-aligned system notice. Legacy wake text without that state stays a user bubble. |
| UI-014 | Fixed, guarded | The main Inspector context kernel mixed subagent cache and child `context_profile` events into the parent occupancy and hit rate, so a long review child made the main conversation look full or highly cached. | `projectContextUsage` ignores `requestKind=subagent`, `Usage.applySubagentUsage`, and the main-kernel reducer test. | Keep the live Inspector and composer meter on the main agent only. Persist subagent totals on the session subagent counters for a later child-owned kernel. |
| UI-015 | Fixed, guarded | The embedded PTY used the chat `--mono` stack, so Powerlevel10k/starship Nerd Font glyphs rendered as tofu, and xterm's default `#000` viewport plus host-padding overflow showed a black hairline at the bottom. | Desktop terminal screenshot, `terminalFontStack` / tab-label tests, and `terminal.css` viewport/scrollbar assertions. | Keep a local Nerd Font fallback stack on the PTY (do not bundle an unlicensed font). FitAddon must measure `.xterm` padding; viewport background and scrollbars use `--paper`/`--ink`, never `#000`. Tab labels are one shell name plus cwd basename. |
| UI-016 | Fixed, guarded | A no-tool Q&A replaced the live 「思考了 Xs」 header with a shorter 「已处理 · 0s」 fold when the stream finished, so the answer jumped; the sparkle also started on a muted token and pulsed to ink. The first-token wait pill later stayed medium-gray because `ThinkingPlaceholder` renders a `disabled` `reasoning-summary` and inherited `button:disabled { opacity: .45 }`. An opacity-only absolute caret later stayed as a gray hairline between settled answer paragraphs. After a tool batch the same wait pill disappeared while the main run was still live, so the transcript looked dead. | Desktop streaming/complete/wait-pill/caret screenshots, `processTrailWorthFolding`, Timeline no-tool remount, between-batch wait, activity-bar identity, and completed-caret tests, and thinking-mark/wait-pill/caret CSS assertions. | Do not fold thinking-only or commentary-only trails. Keep the same thinking header and `data-testid="timeline-prose"` wrapper through completion. In-progress wait → thinking → search/tools stay on one ✦ sparkle bar: only the label changes; do not remount, reset the clock, or switch cards. Reserve the chevron slot so enabling expand does not jump the row. A thinking-only trail stays that sparkle row plus reasoning prose. An opened live step body reads reasoning first as quiet prose, then the tool rows it led to. A settled tool step is a plain count row (`N tool calls` / `N 次工具调用`) with no gray capsule, not the sparkle 「运行了 N 个工具」 bar. Opening it puts one 思考 chip first, then the tools, then file-change pills. Live wait, thinking, and running tools stay on the sparkle bar. Live reasoning stays prose and is not a chip. Do not put those views in a Steps / Reasoning / Search / Coding switcher. Chat thinking chrome uses `--ink` from first paint, including the disabled first-token wait; that wait uses the same left-aligned sparkle row as live and completed thinking, never a separate capsule. Reset that button to `opacity: 1` and never let global `button:disabled` wash it to gray. Shimmer brightness only, never a muted→ink swap. Hide the streaming caret with `content: none` when prose is not `.active`; do not leave an absolute/opacity-only box that can paint between settled paragraphs. A live caret may sit in-flow at the end of the latest block only. Real tool/diff trails may still fold (UI-007). After the last tool/thinking in a live run, keep the 思考 wait bar visible until new commentary, thinking, or a final answer arrives. Empty frames and hidden host fallback are not progress. Do not invent 「我准备」 (UI-008). Every row of that expanded chip list carries a status mark (check, spinner, hollow dot, dash) and one rail segment in a left gutter beside the marks, so an expanded body cannot break the thread. Marks report the real parallel aggregate rather than forcing one active row. New rows cascade at 120ms capped at six; segment IDs stay independent of collection size so appending a block cannot remount the group and replay that entrance, and a virtualized window never re-animates a row it has already shown. One model message plus the work that follows it is one step, and each step is one bar: `ProcessStep` renders `ThinkingState` and `activityBarLabel` owns the wait, thinking, search, running, and settled wording, with the live elapsed clock on that same sparkle bar — individual tool rows do not print a second clock while the run is live. The 思考 label never includes that clock, and the bar hugs the current label instead of reserving a 12em gap. The model's own prose stays in the transcript; its tools, thinking, and file changes live inside that step's body and are never repeated outside it. A trail therefore reads message, bar, message, bar — `ProcessFold` only carries the trail and raises no bar of its own. The body opens from the bar and starts closed, except a live step that has not called a tool yet, which streams its reasoning until the first tool arrives; an explicit collapse or expand always wins. Opening reveals the body with one bounded fade, and the bar's label rolls vertically only when its meaning changes: a ticking clock inside the label must not restart that motion, and the running sparkle breathes on scale and brightness alone. Never hand a finished trail to a second disclosure card, and never let a chip group repeat the enclosing bar's totals — a summarized group prints no header, and an unsummarized one counts only the rows it heads. While the step is live the bar names the row that is executing and changes with it; only a settled step prints the step's own total. Parallel rows report 「正在运行 N 个工具」. The first-token wait and the wait between batches are that same step: the pending step enters the keyed entry list at the position the next step will take, so the element that shows 思考 is the element that later shows the tools. `ProcessStepBar` owns the live clock, because ticking it in `ProcessStep` re-renders the whole body every second and replays row entrances (UI-012). Do not draw a turn-level 正在处理 divider, and do not synthesize an empty 思考 row on send. ChatGPT.app `ma()` in `local-conversation-turn` returns `none` unless the turn is already in progress and then drops Thinking once assistant text exists; there is no Processing line. Only render thinking chrome after the model emits thinking or tools. Keep that sparkle row through completion so the user bubble and final prose stay put. An opened body longer than the deferred minimum stays windowed (UI-012). A bar appearing over an existing trail must not remount it: the body keeps one position whether or not a bar is present. A step with only a delegation card or an empty thinking heartbeat raises no bar while it runs. |
| UI-017 | Fixed, guarded | A settled tool step showed the same run duration on every row (all `1m44s`) because `run_finished` copied the step clock onto tools that had no per-call elapsed time. | Desktop screenshot of five tools sharing `1m44s`, `stampProcessElapsed`, store per-tool clock test, and Timeline distinct-duration test. | Stamp `startedAt` on `tool_started` and each tool's own `elapsedMs` on finish. Never copy the run duration onto a tool. The live sparkle bar keeps the overall clock; individual tool rows stay untimed until the run settles. Each finished tool still stores its own elapsed time. |
| UI-018 | Fixed, guarded | The last lines of a long answer sat under the floating composer, so headings such as 「结论」 could not be read without guessing there was more below. | Desktop screenshot of the composer covering the tail of a numbered list, `transcript-composer-clearance`, and composer overlay-gap tests. | Keep a measured spacer after the timeline instead of padding-bottom on `.transcript`. Floor the overlay gap at `COMPOSER_OVERLAY_MIN_GAP`. Do not let `content-visibility` on the last feed child drop that space from scroll height. |
| UI-019 | Fixed, guarded | The Inspector cache card mixed lifetime aggregate cache totals with the latest main request. Before a new request reported usage it could show an old aggregate rate such as 88%, then switch scope to the latest request and jump to 98%; 「总缓存」 also labeled request input as cache. | `contextCacheMetrics`, Inspector latest-request/pending tests, session `session_1eb44415c423640d3d3602d4` provider-request reconstruction, and browser verification of the cache card. | Compute all three card values from one latest completed main request. Aggregate history must never backfill a missing latest request. While a live run has no report, show a distinct waiting state; preserve unsupported providers as unreported. Label the denominator as request input. |
| UI-020 | Fixed, guarded | Settings and composer controls accumulated several misleading geometry/state defects: a one-level reasoning ladder rendered at minimum and remained focusable, Fast appeared both in the summary and as a separate toolbar lightning button, a grouped Cursor switch changed only the inspected variant, model catalogs could not search raw IDs, route controls escaped half-width cards, settings mixed 9–14px local font sizes, and the fixed 10px Usage heatmap left most of a wide card empty. | User screenshots, `ReasoningEffortSlider` fixed-ladder tests, Cursor family batch action tests, model-catalog search/variant tests, route containment/type-scale assertions, Usage responsive-grid tests, and packaged browser verification. | A one-level ladder renders at the full endpoint with `aria-disabled`, no tab stop, and no pointer/keyboard input. Fast changes only inside the picker and renders externally only as `· Fast` for Cursor and ChatGPT/Codex. Cursor family switches persist every variant atomically; searches retain complete matching families; the catalog lists families only and raw IDs remain search aliases. Keep route controls in bounded card columns, use one enlarged settings token scale, and let Usage heat cells consume the full report width with responsive square cells. |
| UI-021 | Fixed, guarded | Immediately after a cold desktop launch, same-project sidebar rows called generic `execute(resume_session)` and depended on a later runtime event while bootstrap concurrently listed sessions, models, routes, and branches. The initiating window received no direct projection, so an event delay/order race left the visible session unchanged and made the rows appear unclickable. | User screenshot, `Bridge.ResumeSession` direct-readback contract, `CommandPalette` navigation path, `Sidebar` cold-launch regression test, and packaged cold-window click verification. | Same-workspace sidebar and search navigation must call `Bridge.ResumeSession` and apply its returned sequence-0 durable projection directly. Cross-workspace rows rebind the existing GPUI window and load the target daemon's reconnect snapshot. Never make the initiating window depend only on broadcast event timing for navigation. |
| UI-022 | Fixed, guarded | A completed `coding.gofmt` call with structured `changed=false` was classified from its tool name alone as category `edit`, so a process containing one no-op formatter and one build command displayed 「编辑了 1 个文件 · 运行了 1 条命令」 even though no bytes changed. | User screenshot, durable tool record `70560715-9a28-4333-a480-a931a04997ed`, and `does not count an unchanged gofmt result as an edited file`. | Process summaries must inspect structured formatter results. Only `changed=true` counts as an edit; unchanged/failed formatting remains a visible formatter tool row but is summarized as a non-edit tool. |
| UI-024 | Fixed, guarded | Opening a completed pre-answer process applied `max-height: min(56vh, 520px)` plus `overflow: auto` to `process-fold-body`. The outer transcript already scrolls, so long work became a detached nested pane with its own scrollbar and trapped wheel input instead of remaining conversation body. | User screenshot, `process.css`, existing final-answer sibling tests, and `keeps an expanded completed process in the transcript scroll instead of nesting a second scroller`. | The transcript viewport is the only conversation scroll owner. A completed process may collapse under its elapsed-time row, but expanding it restores the complete process to normal document flow immediately before the final answer. Never add a height clamp, `overflow:auto`, or contained overscroll to the expanded fold body. |
| UI-025 | Fixed, guarded | The first assistant-ui Elements adaptation set both `assistant-message` and `assistant-progress` to `max-width: 68ch`. On a wide transcript this left a large empty strip to the right of final answers and process prose even though the transcript column itself was already bounded. | User screenshot and `lets assistant and progress prose use the complete transcript column`. | Let assistant and progress prose use `width: 100%; max-width: none` inside the bounded transcript. The transcript owns readable line width and responsive geometry; child prose must not introduce a second character-based column or leave a dead layout region. |
| UI-026 | Fixed, guarded | Messages still rendered as raw `<article>` blocks while Reasoning, Composer, Tool, Approval, Plan, and Code Block imported renamed Beautiful UI primitives. Styling those files to resemble assistant-ui did not satisfy the component boundary and left the old `bui-*` vocabulary and directory alive. | User message reference screenshot, complete import/class audit, registry Message Pair test, assistant-ui Elements regression suite, and the source-level no-`beautiful-ui` assertion. | Every session turn renders through registry `MessagePairRoot`; user, assistant, progress, and error blocks import the composable exports from `components/elements/message-pair.tsx` directly. Remaining surfaces use `assistant-ui/Elements`, `ToolTimeline`, `ToolTimelineStep`, `CodeBlock`, or other registry-installed components through thin Azem adapters. Keep `data-slot` contracts and `elements.css` as the Azem token bridge. The old component directory and custom `assistant-ui/Message.tsx` must stay absent. |
| UI-027 | Fixed, guarded | Chat diffs used a custom table renderer with line numbers, syntax highlighting, copy controls, a separate vertical scroll cap, and bespoke CSS instead of the requested assistant-ui Code diff element. | `npx shadcn@latest add "@assistant-ui/elements-code-diff"`, `components/elements/code-diff.tsx`, `assistant-ui/CodeDiff.tsx`, adapter and Timeline tests, and packaged visual verification. | Keep the registry-installed CodeDiff as the renderer. The Azem adapter may map `FileChange` records, remove duplicated `---`/`+++` headers, bridge theme tokens, remove the registry `max-w-md`, and add stable `data-kind` hooks. Rows enter in 200ms with a 32ms stagger capped at six; reduced motion stays instant. Do not restore the legacy table/copy/line-number renderer. Horizontal code overflow stays inside the element; vertical scrolling stays owned by the transcript. |
| UI-028 | Fixed, guarded | The composer still wrapped Azem controls in a custom presentation primitive and used a separate `ContextMeter`, so installing assistant-ui Elements had not actually moved input, context, attachment, toolbar, or send chrome onto the registry Composer contract. | `bunx --bun shadcn@latest add "@assistant-ui/elements-composer"`, `components/elements/composer.tsx`, Composer/store tests, source-boundary assertions, and packaged visual verification. | The thread uses registry Composer, ComposerBar, ComposerMenu, ComposerAttachments, ComposerTextarea, ComposerToolbar, ComposerAttachButton, ComposerContext, and ComposerSend. Azem continues to own IME, slash, attachment, approval, plan, model, queue/guide, and cancel behavior. ComposerContext receives the actual ordered `contextComposition` groups with the same localized labels as Inspector; do not synthesize fixed system/tools/messages buckets. The custom Composer export and `ContextMeter` component/CSS must stay absent. |
| UI-029 | Fixed, guarded | ComposerContext collapsed provider-reported usage into fixed `System 0k`, `Tools 0k`, and `Messages 251.2k` rows even though Inspector correctly showed `模型输入 251k` and `当前输出 488`. The UI therefore mislabeled real provider input as conversation messages. | User comparison screenshots, shared `contextCategoryLabel`, `composerContextUsage` category tests, ComposerContext DOM assertions, and packaged hover verification. | Derive ComposerContext segments directly from `contextComposition`, preserve their order and token counts, reuse Inspector localization, and calculate each row's percentage against actual used tokens. Show used and total tokens in the header/footer. Only the ring/progress share uses the context-window limit. Never invent empty categories or relabel `provider_input` as messages. |
| UI-031 | Fixed, guarded | The project tree spent two lines on every project and session, adding a decorative project monogram, branch/path subtitle, project session-count badge, and relative session age. In a narrow sidebar this duplicated context already available in the title bar and Inspector while reducing the number of visible conversations. | User screenshot, `Sidebar` compact-row DOM regression, sidebar source/CSS absence assertions, and packaged desktop verification. | Keep project rows single-line with only expand/collapse, project name, and project-scoped new-conversation action. Keep session rows single-line with status dot, title, and real running/unread state. Do not restore project monograms, branch/path subtitles, project count badges, or session-age labels; Pull Request rows retain their own explicit status. |
| IPC-001 | Fixed, guarded | A renderer-owned desktop runtime necessarily stopped active provider streams, tools, subagents, and PTYs when that renderer process closed; a native replacement also needed to avoid base64 attachment/terminal copies and unbounded replay. A first reconnect path replayed imperative `open_project` events into each child renderer, could apply transcript events after the snapshot that already contained them, and omitted pending controls after replay eviction. | `TestClientDetachLeavesDaemonAvailableForReconnect`, IPC authentication/codec/replay/transfer/terminal tests, `replay_sequence_boundary_distinguishes_live_events`, pending-control projection tests, Rust reconnect/state tests, strict Clippy, IPC benchmarks, signed `Azem-GPUI.app`, and packaged launch verification. | Keep one daemon per canonical workspace, the existing Bridge as the only desktop operation set, owner-only endpoint/token/Unix socket or owner/System Windows pipe, nonce/client/workspace/version HMAC authentication, bounded control/binary frames, SHA-256 attachment commit, lossless incremental text, byte-bounded replay with explicit resync, terminal replay, durable reconnect snapshots, and detach-without-cancel semantics. Apply in-flight events before the snapshot, suppress stale replay by connection high-water while retaining actionable controls and AgentID streams, include pending approvals/questions in snapshots, rebuild durable question/plan controls, keep the selected snapshot session in an atomic target, serialize complete terminal replay transfers, and never re-execute replayed `open_project`. Explicit daemon stop must refuse an active main run unless `--include-active` is supplied. |
| UI-032 | Fixed, guarded | The first GPUI renderer proved the daemon/IPC architecture but was not a usable desktop replacement: it exposed a generic navigation list and raw JSON, native text fields did not explicitly take focus on click, macOS traffic lights overlapped the conversation switcher, and the required process trail, diff summary, icon vocabulary, Environment panel, model catalog, and settings dialog were absent. A later synchronous reconnect catalog also made reproduced durable-state readiness take 5.419 seconds, and nullable Bridge slices caused valid `model_providers` events to be dropped. | User reference screenshots, packaged GPUI light-mode and Settings captures, `nullable_bridge_collections_do_not_drop_model_provider_events`, reconnect deferral tests, measured 203-244 ms window creation and 1.196-1.606 s cold durable-state readiness, strict Rust tests/Clippy, signed bundle launch, native input focus path, and full Go/GPUI gates. | Keep GPUI presentation aligned with the validated native shell: one integrated transparent macOS titlebar with a 37 px traffic-light-safe region; the same sidebar/project/session/ellipsized-PR structure; focused and range-safe IME inputs; bundled licensed Lucide and complete models.dev provider logos; run-grouped thinking/tool/diff folds; the 288 px Environment panel; structured PR views; and the rounded Settings modal with provider inventory, subscription quota, model enable controls, routes, and capability metadata. The first reconnect response contains durable session/project/run/terminal state only; optional catalogs stream after first paint, provider inventory precedes remote model refresh, and JSON `null` collection fields are normalized to their default before typed reduction. Do not replace these with generic raw-JSON cards, separate titlebar chrome, synchronous optional catalogs, or blank provider marks. |
| UI-008 | Fixed, guarded | Providers could start single or batched tools without first giving the user a progress update, while adjacent reasoning rendered as separate zero-second rows instead of part of the announced step. After the host fallback was hidden, each thinking span or fallback-glued batch became its own ✦ 思考了 row and replaced the model's 「我准备…」 prose. | Main prompt contract, provider sink fallback/order test, `groupProcessTimelineBlocks`, and active Timeline regression. | Visible commentary is only model-authored ordinary prose (`TextPhase=commentary`). Never invent 「我准备」 or un-hide `synthetic=tool_announcement`. Group adjacent thinking/tools/diffs under that announcement, or into one thinking trail when the model omitted commentary. Adjacent thinking spans share one ✦ 思考了 row when there are no tools (sum of thinking time or the group span), not one row per span. While that step is live, search/tools only retitle the same sparkle bar. After it completes, expand to one chip list with thinking as the first chip. Keep nested work visible. Do not render commentary as a titled duration card. Live wait between tool batches must stay visible without host boilerplate (UI-016). |
| UI-009 | Fixed, guarded | Durable session recaps were emitted by the backend but discarded by the desktop reducer, so the right Inspector could not restore or live-update continuity state; recap generation also reused the old semantic compaction route. | Recap reducer/Inspector tests, independent model-route configuration tests, and provider runtime route tests. | Keep `session_loaded` and `recap_state` projected into one current-session recap card. Preserve an independent `agents.recap` route; changing the recap writer never changes the current conversation or deterministic context archive. |
| UI-010 | Fixed, guarded | Thinking, tool-step, and tool-summary markers kept a paper-colored rail mask while their parent row switched to the muted hover surface, producing a detached white circle around the marker. | Process-rail CSS regression test and real browser hover verification. | Preserve the rail mask at rest, but remove its background and shadow for every highlighted process-row marker. |
| NETWORK-001 | Fixed, guarded | Finder-launched Azem ignored the active macOS system proxy because Go's default HTTP transport reads proxy environment variables but not SystemConfiguration. ChatGPT requests connected directly and failed with `EOF` or TLS handshake timeouts while Codex/Electron succeeded through the local proxy. | `internal/netproxy`, auth/provider transport tests, live ChatGPT proxy test resolving `127.0.0.1:6152`. | Keep desktop bootstrap and every Azem-owned HTTP transport on the shared resolver. Preserve scheme-specific environment overrides, native proxy refresh, bypass rules, streaming zero-timeout semantics, and the opt-in live endpoint test. |
| AUTH-001 | Fixed, guarded | Grok device login created and polled device codes without the current Grok Build request metadata, so xAI could reject the grant with `device token request returned HTTP 400: invalid_grant`. | `internal/auth/grok/client.go`, `TestDiscoveryAndDevicePolling`, `TestRefreshSendsClientHeadersAndIncludesErrorBody`, and the Grok Build device-flow contract. | Keep `referrer=grok-build` on device-code creation and send the shared client-version plus `x-grok-client-surface=ui` headers on device creation, polling, refresh, and revoke. Do not retry `invalid_grant` as `authorization_pending`. Surface the OAuth error body on refresh HTTP failures. |
| AUTH-002 | Fixed, guarded | Connected Grok settings showed `anonymous-<hex>` and a red 获取失败 because device tokens have no top-level `account_id`/`email`, quota sent that hash as `x-userid`, and the UI hid `quotaWarning`. A later `/v1/user` fetch could also fail with `EOF` when an HTTP/2 stream through the desktop proxy was dropped. A TLS handshake timeout was not classified as transient, so the one-shot quota refresh cached the error and settings never refreshed it. Live `/v1/billing?format=credits` then omitted `creditUsagePercent` on a unified weekly period, so Azem failed with `no weekly quota` while CodexBar showed 0% used from `onDemandUsed`/`onDemandCap` or a zero-usage current period. | JWT identity extraction, `/v1/user` then `/v1/billing?format=credits`, `TestGrokQuotaUsesLiveUserIDAndSurfacesHTTPReason`, `TestGrokQuotaRetriesTransientUserEOF`, `TestSubscriptionQuotaTimeoutRefreshesUntilSuccess`, `TestDecodeSubscriptionQuotas`, SettingsDialog quota-reason test. | Keep `/v1/user` as the only billing `x-userid` source. Persist email/display on the existing account id; do not rename stored anonymous ids. Surface `quotaWarning`. Retry EOF, connection reset, deadline, and `net.Error` timeouts with a fresh request timeout. After a retryable quota failure, keep one account-scoped refresh active with capped backoff, clear the stale warning while each retry is running, and stop on success, a non-retryable error, account replacement, or shutdown. Accept omitted `creditUsagePercent` via on-demand ratio or a parseable current period at 0%. Do not regress AUTH-001 headers or skip `internal/netproxy`. |
| AUTH-003 | Fixed, guarded | Harbor Terminal-Bench trials copied a snapshot of the Grok refresh token into an isolated container. The first trial rotated that token; later trials reused the host snapshot and failed immediately with `azem-eval: refresh returned HTTP 400`. | Job `tb2-azem-grok-4.6-xhigh` (2/21 then 19× `NonZeroAgentExitCodeError`), `cmd/azem-eval` `--refresh-auth`/`--sync-auth-*`, Harbor adapter write-back, and `TestSyncAuthCopiesNewerGrokCredentialWithoutOpeningDestAsRuntimeStore`. | Refresh Grok tokens on the host before each trial upload. After the trial, merge newer credentials back into the slim eval db and the desktop db with raw SQLite (do not `sqlitestore.Open` the user database). Do not copy `reauth_required` over a still-valid dest token. Prefer one concurrent Grok-subscription trial so two containers cannot rotate the same refresh token. |
| EVAL-001 | Fixed, guarded | Bare Ubuntu Terminal-Bench images have no `ca-certificates`, so `azem-eval` died on `tls: failed to verify certificate: x509: certificate signed by unknown authority` before any work. Harbor then scored those trials 0 with `NonZeroAgentExitCodeError` instead of a real verifier result. | Job `tb2-azem-grok-4.6-xhigh-2` `dna-assembly` / `regex-log`, stock `ubuntu:24.04` missing `/etc/ssl/certs`, and `path-tracing` succeeding only because it installs `curl`. | Upload the host CA bundle into every trial, install it under `/etc/ssl/certs`, and set `SSL_CERT_FILE` for `azem-eval`. Do not skip TLS verification. |
| EVAL-002 | Fixed, guarded | The Harbor adapter ran `azem-eval --timeout 0` and never passed `timeout_sec` into `exec_as_agent`. Harbor still wraps `agent.run` with `asyncio.wait_for` using `task.toml` `[agent] timeout_sec` (900s / 1200s / 3600s). The turn therefore ran until Harbor cancelled `docker compose exec` mid-read and recorded `AgentTimeoutError`, including on tasks whose verifier later scored 1.0. | Job `tb2-azem-grok-4.6-xhigh-fails-2` (six `AgentTimeoutError` trials whose `agent_execution` windows equal the task timeout), `eval/harbor/timeouts.py`, and `timeout_test.py`. | Recover Harbor's computed budget from constructor kwargs, `AZEM_EVAL_TIMEOUT` / `HARBOR_AGENT_TIMEOUT_SEC`, or trial `config.json` plus `task.toml`. Pass that budget minus wrap slack to `azem-eval --timeout` and `exec_as_agent`. Treat an eval/docker timeout as a completed attempt so Harbor can score the workspace. Do not restore `--timeout 0` on Harbor trials when the budget is known. |
| EVAL-003 | Fixed, guarded | Schema-21 blob offload stores `{}` in JSON columns such as `session_blocks.data`, `events.data`, and `records.data`, but the trajectory exporter recognized only a zero-length spill sentinel. Real completed eval sessions therefore failed export with `inline payload … disagrees with stored digest` even though the referenced blob and digest were valid. | Live isolated core/recovery/Todo databases, corrected exports `core-v3-trajectory.json`, `recovery-trajectory.json`, `todo-v5-trajectory.json`, and `TestExportTrajectoryPreservesDurableRowsAndExternalPayloads`. | Treat only the defined empty-byte and `{}` inline values as spill sentinels when a valid non-empty digest points to BlobStore. Recover and SHA-256-verify the blob. Continue rejecting every non-sentinel inline/digest mismatch and every missing or corrupt blob. |
| PR-001 | Fixed, guarded | GitHub PR capability needs a visible entry point and explicit prerequisites. | README Pull Requests section, `internal/githubpr/`, desktop PR pages. | Keep README synchronized with mutations, head-OID protection, Monitor & Fix triggers, and unavailable states. |
| PR-002 | Documentation gap | PR operations depend on local `git`, GitHub CLI authentication, a resolvable GitHub remote, and repository permission. | `internal/githubpr/client.go`. | Document missing `gh`, logged-out, non-GitHub, permission, network, and rate-limit failures. Never represent unavailable capability as an empty list. |
| PR-003 | Documentation gap | PR monitoring polls, persists state, deduplicates fingerprints, and starts repair sessions; its maintenance contract is not independently documented. | `internal/githubpr/monitor.go`: 60-second interval, five-minute maximum backoff, state version 3. | Document the state machine, persistence file, trigger, retry, deduplication, concurrency, restart, and stop behavior. |
| PR-004 | Documentation gap | Merge, auto-merge, reviews, and other mutations have external side effects without a dedicated permission and safety guide. | `MutationRequest` in `internal/githubpr/types.go`. | `docs/security.md` must list each mutation, permissions, validation, head-OID protection, and audit evidence. |
| DOC-001 | Documentation gap | Core architecture, persistence, testing, configuration, provider streaming, security, agent-runtime, and recovery guides now exist, but PR, release, troubleshooting, accessibility, contribution, changelog, and ADR documentation remain. | Existing `docs/*.md` files and documentation index below. | Complete the remaining index incrementally; keep README as the short user entry point. |
| DOC-002 | Fixed, guarded | Project Layout previously omitted the native desktop, daemon, IPC, runtime-js, and GitHub PR packages. | README Project Layout. | Update Project Layout whenever a top-level application or module boundary changes. |
| DOC-003 | Fixed, guarded | Development documentation previously covered only Go tests and formatting. | `docs/testing.md`, README Development section, `Makefile`. | Keep Go, GPUI, desktop IPC, SQLite, architecture, and GUI smoke commands synchronized with the real build. |
| SEC-001 | Fixed, guarded | Combined GitHub, OAuth/API-key credential, hook, MCP, and automated-repair risks previously lacked a full threat model. | README Security Model and `docs/security.md`. | Keep the threat model synchronized with every new Bridge action, credential path, external mutation, hook, MCP, and automated-repair capability. |
| OPS-001 | Unresolved | There is no complete database upgrade, backup, rollback, corruption recovery, or downgrade runbook. | Schema 17/18 incident; automatic `.bak` implementation. | Write `docs/release.md` and `docs/troubleshooting.md`; prohibit downgrade writes. |
| PARITY-001 | Fixed, guarded | Azem's initial frozen OMP v18.0.3 audit recorded partial or missing behavior across tools, agent modes, extensibility, sessions, protocols, and operator workflows. | `internal/parity/manifest.json`, `TestManifestLoadsValidatedOMPBaseline`, cross-repository provider contracts, real coding fixtures, protocol suites, GPUI checks, and zero `.sentrux` violations. | Keep the baseline pinned to OMP commit `160ed439ac0df594347e7d7018b813a7ffdb5e81`. Every capability remains `complete` or `stronger` with current source evidence; new OMP baselines require a new audit rather than silently changing this one. |
| TEST-001 | Fixed, guarded | Maintainers previously lacked a unified command matrix by change type. | `docs/testing.md`, verification matrix below. | Keep commands executable and require a real GUI launch for desktop behavior changes. |
| UI-033 | Fixed, guarded | Native single-line search text sat above its icon because shared input padding was top-aligned. Carets never blinked and clicking a plain header left the search field focused. | `caret_requires_active_focus_and_visible_blink_phase`, native outside-click input reproduction, and consecutive signed-bundle captures of catalog, route, and composer caret phases. | Keep single-line text vertically centered, multiline text top-aligned, and small gray carets gated by both input focus and active window. Reset the 500 ms blink after editing, cancel it on blur, and clear focus on an outside primary click. Blink notifications must not rerun model filtering. See `docs/desktop.md`. |
| UI-034 | Fixed, guarded | Native Appearance and Security Settings used static decorative controls; the theme ignored user choices, security parameters had no save path, and failures were visible only in Extensions. Primary buttons inverted body text colors and turned white in dark mode. | `explicit_theme_overrides_system_and_system_keeps_following`, `appearance_changes_validate_and_survive_restart`, `primary_buttons_use_dark_surfaces_in_both_themes`, `settings_search_matches_names_and_control_keywords`, `security_form_validates_fields_and_only_sends_desktop_owned_keys`, and isolated signed-bundle save/readback. | Keep native appearance persistence, real system-font selection, functional keyword search, and palette-aware placeholders/buttons. Security edits use a dirty draft, validate the eight desktop-owned fields, save only on explicit action, preserve protected backend configuration, and never start a scan. Render failures across every Settings category. See `docs/desktop.md`. |
| UI-035 | Fixed, guarded | The native project context menu exposed Finder, archive, and copy actions but could not remove a remembered project from the app. The first implementation then reused `TouchProject` for both startup accounting and explicit reopen, so every restart set `visible=1` and resurrected the removed row. Deleting `desktop_projects` directly would violate session ownership and its restrictive foreign key. | User screenshots/restart reproduction, `TestHiddenProjectLeavesOwnershipSurvivesTouchAndReappearsOnRestore`, `TestHiddenProjectSurvivesDatabaseReopenAndStartupTouch`, `TestRemoveProjectHidesCatalogWithoutDeletingOwnedSessions`, and native Sidebar action tests. | 「从 Azem 中移除」 changes only schema-28 catalog visibility. Startup `TouchProject` preserves an existing visibility choice; only explicit `RestoreProject`, opening the path, or creating/resuming owned work restores visibility. Never delete workspace files, sessions, or `session_workspaces`. Removing the active project switches the existing window to another visible project; the only active project cannot be removed. |
| UI-036 | Fixed, guarded | One model run could show two 「正在处理」 rows because a streaming thinking block stayed active after the next tool started. Tool rows also repeated 「正在运行」 in their text, lacked a distinct live marker, and represented failures only with red color; neither failed nor successful calls exposed their result, error, or edit diff. | User screenshots, durable run `run_6e3ce629627ab708c77b46d1`, its commentary/Todo/approval evidence, `tool_activity_settles_prior_streaming_thinking`, `tool_rows_show_status_and_failure_disclosure`, and `successful_tools_expose_results_and_edit_diffs`. | Settle prior streaming assistant/thinking blocks when later text or a tool starts, so only the latest run activity remains live. Keep action labels free of redundant running prefixes and use shape plus motion for status. Every tool with bounded detail is a keyboard-focusable disclosure: failures show the recorded reason, successful calls show their result, and edits prefer the structured compact diff. Details stay in transcript flow without a nested scroller. Clickable rows remain visually flat and never add a full-row hover or selection capsule around selectable text. |
| UI-037 | Fixed, guarded | Intermediate or legacy-unphased assistant prose received copy/feedback/fork controls after every paragraph, shell output was parsed as Markdown and split source lines into separate list/code capsules, and an out-of-order progress event could leave rows titled 「工具调用 running」 or without a target. | User screenshots, `reply_footer_only_follows_the_terminal_final_answer`, `successful_tools_expose_results_and_edit_diffs`, and `late_tool_start_repairs_an_unnamed_progress_row`. | Render reply controls only for the last completed `final_answer` block of its run. Render command and test output as one safely fenced code block, never as arbitrary Markdown. Repair an existing progress row when its start event supplies the tool name, ignore bare transport status words as targets/details, classify `coding.go_test` as a localized test action, and prefer decoded arguments such as `package` over result text in row labels. |
| UI-038 | Fixed, guarded | Native standalone completed thinking used a one-off lightbulb row and was the only settled process element labeled with its elapsed time, producing a lightbulb plus 「思考了 1 分钟」 while tool-backed thinking used plain process summaries. | User screenshot and `thinking_row_has_no_unique_icon_or_completed_duration`. | A standalone live thinking row may use the ordinary processing label for activity, but once settled it becomes plain 「思考」. Do not render a special leading icon or completed-thinking duration; tool and run timing remain owned by their normal process summaries. |

Public issue state is not evidence that the product has no defects; the
evidence-backed internal record above remains the maintenance backlog.

## Product Contracts That Must Not Regress

### SQLite and upgrades

- `schemaVersion == len(migrations)`.
- Runtime migrations and `internal/store/sqlite/dbgen/schema.sql` stay aligned.
- Preserve the automatic backup before upgrading an existing database.
- Never delete or rebuild a user database to solve a version conflict.
- Test previous-version upgrade, current-version reopen, retained data, and
  safe rejection of a future schema.
- Hold the shared runtime recovery fence until the process has stopped its
  agents and closed persistence. A second desktop window must never recover or
  quarantine work owned by another live process.
- Schema 18 contains `agent_definition_snapshots`, `admission_reservations`,
  `resource_claims`, and every index defined by migration 18.
- Schema 19 contains `desktop_projects`, `session_workspaces`, and the
  `session_workspaces_workspace` index. Projects are application state, not a
  single `workspace.root` configuration value.
- Schema 20 contains semantic state, append-only semantic events, and context
  manifests. Its migration invalidates legacy replaceable ModelHistory/cache
  identity but preserves canonical transcript and all other durable stores.
- Schema 21 stores large Azem payloads in `~/.azem/blobs` and
  keeps only catalog rows plus SHA-256 digests in SQLite. It extracts
  artifact bytes, large tool/subagent/model-history payloads, large
  non-FTS transcript blocks, and Venat `events`/`records` bodies larger
  than 4 KiB, then drops `context_artifacts.payload` and the leftover
  `session_projections.blocks` column. Event/record catalog rows, FTS
  indexes, and user/completed assistant block JSON stay in SQLite.
- Schema 22 contains `llmux_provider_models`. llmux discovered and imported
  model catalogs live there; `config.yaml` keeps only provider `enabled` and
  `base_url`. Legacy YAML `models:` lists are imported once on launch.
- Schema 23 contains native security scans, workers, progress, artifacts,
  findings, occurrences, locations, triage, remediation, matching, and
  publication receipts. Canonical report bytes remain private scan-local files.
- Schema 24 contains `session_graphs`, parent-linked `session_graph_entries`,
  named `session_branches`, and `session_labels`. Appending a canonical block
  advances the active leaf transactionally; navigation never deletes another
  branch.
- Schema 25 contains auth-broker access tokens, disabled credentials,
  temporary account blocks, and usage observations. Broker credentials remain
  server-side; clients persist only encrypted snapshots and read-only account
  projections.
- Schema 26 contains signed GitHub webhook delivery receipts. Delivery IDs and
  payload digests make redelivery idempotent before a repair trigger is
  admitted.
- Schema 27 contains `agent_executions`, `agent_effect_attempts`,
  `agent_execution_receipts`, and `agent_execution_bindings`. Every v1
  binding identifies one Azem session/run/agent/kind/segment and one immutable
  executable manifest. Large manifest/checkpoint/result payloads use BlobStore
  digest verification. Non-terminal v0.15 rows without a binding become
  `reconcile_required`; they are never replayed speculatively.
- Schema 28 adds `desktop_projects.visible`. Hiding a project removes only its
  catalog projection; `session_workspaces` ownership and workspace files stay
  unchanged. Startup touches preserve the flag; only an explicit reopen restores
  visibility.
  The migration also marks pre-fix non-terminal `subagent_runs` as interrupted
  so stale roster entries cannot survive the upgrade.


### Frozen OMP parity baseline

- `internal/parity/manifest.json` pins OMP v18.0.3 commit
  `160ed439ac0df594347e7d7018b813a7ffdb5e81`.
- Every in-scope capability must remain `complete` or `stronger` and carry
  current Azem evidence. `OpenCapabilities()` returning a row is a release
  blocker.
- Public-library API compatibility and OMP visual identity remain outside the
  frozen scope. User-visible coding-agent and operator behavior do not.
- A newer OMP release changes the comparison baseline only through an explicit
  manifest audit with matching tests and documentation.

### Archive-first context compaction

- Automatic triggers, manual `/compact`, `/rebuild`, Main, Team, and subagent
  paths use one host-built deterministic archive. No `agents.compaction` route,
  provider request, background prepare, or model-written semantic state exists.
- The archive source is a session-scoped `context_archive` artifact with a
  verified SHA-256 digest. Keep the latest three complete shared user turns
  verbatim, keep tool groups atomic, and expand an older source before selecting
  a new boundary.
- `keep_recent_tokens` is a preferred hot-tail floor. If it prevents every
  carrier from fitting, relax only that optional floor and retry while
  preserving the latest three complete shared user turns.
- Vision-capable routes may receive at most eight 1568×1568 bitmap frames and
  4 MiB total frame payload. Text-only/unknown routes and over-budget frames
  receive the bounded artifact carrier. PNG is optional and repairable from
  the source artifact; it is never the only copy.
- Archive source loss/digest mismatch, artifact persistence failure, and an
  oversized latest-three-turn suffix fail explicitly. Never clear state,
  silently truncate a complete recent turn, or persist a partial checkpoint.
- Keep `ArchiveContextManifestV1` policy v3, `ModelHistory` wire v3,
  source/frame SHA validation, transactional activation, stale-checkpoint
  rebuild, Inspector diagnostics, repeated expansion, and restart frame-repair
  coverage.

### Provider text phases

The complete path must preserve `TextPhase`:

```text
provider stream
  -> app runtime event
  -> desktop bridge/session persistence
  -> GPUI state reducer
  -> timeline rendering
```

- `commentary` explains work around tool calls and is not a final answer.
  Real model commentary is ordinary prose in the transcript, not a titled
  duration card. The executable main prompt requires the model to author
  that commentary itself; the host must not invent 我准备 text. The host
  fallback announcement is an internal grouping anchor and must not render
  as user-visible prose.
- `final_answer` is the user-visible terminal response and appears once.
- Reasoning/thinking never impersonates commentary.
- Persisting and reopening a session preserves phase and order.
- UI projection compaction never fails the provider run. Consumers reload the
  durable session projection after replaceable deltas are discarded, including
  one final reload after the terminal event.

### llmux providers

- `chatgpt` and `grok` remain reserved for their existing subscription drivers.
- llmux profile IDs resolve only when enabled and when the selected model exists
  and is not disabled in `providers.llmux.<id>.models`. Subscription
  `disabled_models` follows the same runtime and model-picker rule; Settings
  keeps disabled models visible so they can be re-enabled.
- Stored API keys use `internal/auth`; YAML and runtime events never contain
  them. Environment fallback uses the profile's declared variable.
- llmux internal retries remain disabled so Azem's `retryProviderDriver` is the
  single pre-stream retry owner. Durable unknown attempts are reconciled, not
  retried.
- Image attachments retain the shared trusted-root, regular-file, and detected
  content-type validation before entering llmux. Do not impose a product-wide
  image count or per-image byte cap below the selected provider's own contract.
- Image-capable main models receive validated attachments directly. A main
  model whose catalog explicitly excludes images must use the independently
  configured `agents.vision` route; its bounded output enters the main context
  as private, untrusted user evidence and the text-only main request contains
  no image parts. Unknown modality metadata keeps the native compatibility
  path, and a missing or unusable helper fails explicitly.

### Venat integration

- `go.mod` pins Venat v0.16.1 and is authoritative; do not rely on a local
  `go.work` or adjacent checkout. Release/final validation uses `GOWORK=off`.
- Construct one `durable.Runtime` for the complete app-service lifetime.
  Main, each Team role, each Subagent, and each automation request execute
  through a direct `agent.Engine`; no legacy Runner, worker, deployment, or
  store compatibility path remains.
- Seal the immutable executable manifest and v1 binding before an execution can
  open a provider stream or tool side effect. Preserve
  provider/account/model/reasoning, Skill/tool schema, workspace, static
  instruction, and route identity.
- Azem owns runs/tasks/approvals/resource policy, Team/Subagent orchestration,
  retry policy, UI projection, and user-visible lifecycle. Venat durable owns
  one execution's fenced lease, continuation/checkpoint/result, versioned
  model/tool attempt, and idempotency receipt. Never create a second owner.
- Recovery starts pending, leaves suspended approval waiting, resumes an exact
  continuation, replays terminal result, and surfaces unknown attempts for
  explicit reconciliation. Approval resume names the exact durable operation.
- Run the pinned upstream `agent`, `message`, `provider`, `tool`, `skill`,
  `orchestration`, and `durable/...` contract tests on every Venat upgrade.

### Tool dispatch concurrency

- Main, Team-role, Subagent, and automation engines use parallel tool dispatch.
- The executable main prompt requires a durable `todo` snapshot before any
  workspace investigation or modification. Trivial chat that needs no
  workspace work is exempt.
- `todo init` accepts only goal, phase titles, and item content. The host
  assigns every phase/item ID and status; model-authored identity or state is
  rejected.
- Subagents may recursively delegate up to `agents.subagents.max_depth`; the
  default is 2, `-1` is unlimited, and 0 disables delegation.
- Subagent concurrency defaults to 32; zero is unbounded. A full scheduler may
  admit one re-entrant child per active parent to avoid recursive wait deadlock.
- Soft request budgets are advisory only and never terminate a child.
- Shell execution and subagent scheduling continue to enforce their independent
  configured concurrency limits.
- Skill activation batches remain sequential because later calls can depend on
  tools registered by the activation call. Finish resource reads before
  launching independent foreground Subagents in the following parallel batch.
- A later turn in the same session must treat completed
  `hydaelyn_activate_skill` records as already active when the skill is still
  resolvable. Disabled or deleted skills are not replayed. Reading a skill
  resource that was never activated, or an undeclared path such as `SKILL.md`,
  returns an error tool result and must not fail the run (SKILL-003).
- Review or verification that gates later work stays foreground, or the parent
  must consume it with `subagent.get_output` before ending the turn. A parent
  that still has unfinished children of the current run keeps receiving
  `pending-background-children` retries until every child is terminal. The
  host never cancels those children for slowness; `idle_timeout` may cancel a
  silent child (SUBAGENT-001, SUBAGENT-004, SUBAGENT-005).
- The durable session Todo completion gate applies only to the parent. A child
  verifies its own changed-path evidence but must not inherit the parent Todo;
  the parent still waits for that child through `pending-background-children`
  (SUBAGENT-008).
- `agents.subagents.idle_timeout` defaults to `5m`. Zero disables the
  watchdog. A positive window cancels only a running child with no thinking,
  output, or tool activity, no open tool, and no live `coding.shell`. Empty
  thinking/text frames and elapsed-time UI ticks are not activity. Compaction
  and explicit wait summaries reset the clock (SUBAGENT-005).
- Desktop stop is an explicit include-children stop. Parent-wait expiry and
  the TUI parent-only choice still detach safe children instead of
  cancelling them (SUBAGENT-001, SUBAGENT-006).
- Background completion auto-wake batches every undelivered terminal child in
  the session into one turn, except children of a parent run that just
  completed successfully. The wake user block keeps `kind=user` with
  `state=subagent_wake` (UI-013).
- The live Inspector context kernel and composer occupancy meter count only
  the main agent. Subagent `context_usage` and child `context_profile` events
  must not replace those totals (UI-014).

### Native GPUI IPC

- Native interface translations live in `gpui/assets/locales/*.json`, with
  optional user packs in `$AZEM_HOME/locales` (default `~/.azem/locales`). Discover
  languages from files, not a fixed language enum or inline bilingual strings.
  Keep bundled key/placeholder parity, bounded validation, English fallback,
  acknowledgement-driven language changes, and draft-preserving placeholder
  refresh. See `docs/desktop.md`.
- `internal/daemon` owns one `internal/app` desktop composition per canonical
  workspace. GPUI is a renderer over that runtime; it must never implement a
  second provider, agent, approval, persistence, or recovery path.
- IPC protocol version, methods, actions, event kinds, frame limits, and Rust
  contracts come from `internal/desktopipc/protocol.go` and
  `cmd/gen-contracts`. `make contracts-check` must stay clean.
- Endpoint/token files and Unix sockets are owner-only. Windows named pipes
  allow only the current owner and LocalSystem. Authentication binds nonce,
  client ID, workspace ID, and protocol version with HMAC-SHA256.
- Control frames remain at most 16 MiB. Binary chunks remain at most 256 KiB;
  attachment reassembly remains at most 256 MiB with exact chunk order, size,
  and SHA-256 verification.
- Incremental text and thinking are lossless. Only cumulative snapshots may
  coalesce while pending. Replay and client queues are byte-bounded; overflow
  emits `resync_required` and reconnect restores durable projection state.
- `client_detach`, window close, and renderer crash never cancel the runtime.
  Explicit daemon stop is separate and refuses an active main run without
  `--include-active`. Daemon shutdown still closes PTYs, Bridge, agents, and
  persistence in order.
- Terminal output stays binary, is parsed through Alacritty VTE state, and has
  an independent 4 MiB replay bound per terminal. Attachments never use base64
  on the GPUI wire.

### Plugins

- `.codex-plugin/plugin.json` is the required plugin entry point; every
  referenced path begins with `./` and remains inside the plugin root.
- Load only from Azem's `plugin-packages` data directory. Direct packages live
  under `local`; Codex paths are copy sources only and imported packages live
  under `codex/<marketplace>/<plugin>`.
- List packages reported installed by the current Codex catalog, but import
  only IDs explicitly selected in `plugins.codex_imports`. A failed later
  import must not remove the last valid selected Azem copy.
- Unselected Codex plugins contribute no runtime Skills, MCP servers, Hooks, or
  Apps. Plugin icons are read from Azem copies, bounded and type-checked, then
  projected as image data instead of local filesystem paths.
- Plugin Skills and MCP servers reuse the existing runtimes; do not create a
  second execution or approval path.
- Installing or enabling a plugin does not trust its Hooks. Keep
  `plugins.trust_hooks` false by default and provide `PLUGIN_ROOT`,
  `CLAUDE_PLUGIN_ROOT`, and `PLUGIN_DATA` only after explicit trust.
  Catalog plugin hook files before trust; execute them only when trusted
  and not listed in `hooks.disabled`. `set_hook_enabled` is the only
  per-hook control and reuses the existing dispatcher.
- OAuth-only MCP and `.app.json` connections remain visibly unavailable until
  a separately authenticated connection exists.

### Native security scanning

- Security audits use Azem provider drivers and Venat; never add a Codex CLI,
  Node, Python, second scheduler, hidden conversation session, or second scan
  database.
- Audit models and their security-only child roles read an immutable private
  snapshot with read-only tools. Progress paths require successful read
  receipts, and complete claims with missing receipts become partial. Models
  submit semantic drafts only; target/finding scope binding, stable IDs, Schema
  validation, report/SARIF, diff-evidence sealing, and completion are host owned.
- Deep Scan persists independent complete audits, usage, and one serial reducer.
  Native scans set no Token/tool-call hard ceiling. Absolute-deadline/error
  stops preserve completed work as partial coverage; zero completed source
  review is never a complete no-findings result.
- Patching requires a completed non-stale scan and clean Git checkout, edits an
  isolated worktree only within host-accepted finding locations, and needs an
  independent non-editing verifier with read/test receipts before commit or PR
  publication.

### GitHub pull requests

The complete PR contract includes capability detection, dashboard lists,
details, checks, reviews, comments, commits, files, activity, every mutation,
external links, failure/conflict monitoring, fingerprint deduplication,
isolated repair sessions, and these states: `disabled`, `watching`, `pending`,
`repairing`, `completed`, `error`.

Security requirements:

- Execute GitHub CLI with argv; never assemble a shell command.
- Validate user input at the existing client boundary.
- Respect repository merge methods and permissions.
- Return explicit success or failure for remote mutations.
- Never start duplicate repairs for the same failure fingerprint.

## Documentation Index

### Existing documentation

| Path | Status | Purpose | Follow-up |
|---|---|---|---|
| `AGENTS.md` | Current | Agent rules, regression record, documentation index, verification matrix. | Update issue and document state changes. |
| `README.md` | Current user entry | Quick start, features, configuration, security overview, project navigation. | Keep concise and link detailed guides. |
| `docs/architecture.md` | Complete | Shared runtime, package boundaries, event flow, key call paths, extension points. | Update for module or event-boundary changes. |
| `docs/persistence.md` | Complete | Paths, schema, SQLC synchronization, backup, compatibility, durable stores, recovery. | Add schema ADR. |
| `docs/testing.md` | Complete | Test levels, command selection, desktop smoke test, live-test conditions. | Update whenever commands or build targets change. |
| `docs/desktop.md` | Complete | GPUI/daemon startup, IPC and desktop-operation boundaries, workspace limits, build, and smoke checks. | Update for new desktop operations, windows, navigation, or IPC behavior. |
| `docs/agent-runtime.md` | Complete | Venat v0.16 direct engines, durable execution/binding ownership, Team/Subagent scheduling, approvals, recovery, usage, budgets. | Update for Venat upgrades, execution identity, scheduling, budget, or resume-contract changes. |
| `docs/recovery.md` | Complete | Pre-open recovery fence, legacy preparation, v1 state classification, reconciliation, tool timeline continuity, Team/Subagent restore, suspension, process lifetime. | Update for recovery, reconciliation, or run-state changes. |
| `docs/context-rebuild-plan.md` | Complete | Deterministic context archive kernel, safe cuts, artifact/bitmap carriers, persistence, recovery, and failure semantics. | Keep synchronized with context policy and schema changes. |
| `docs/plugins.md` | Complete | Shared Codex plugin standard, compatibility matrix, discovery, trust, and runtime projection. | Update when the upstream manifest or supported capability boundary changes. |
| `docs/security-scanning.md` | Complete | Native target snapshots, Standard/Deep runtime, contracts, findings, remediation, UI, publication, and verification. | Update with every security-scan behavior or schema change. |
| `CHANGELOG.md` | Current | User-visible compatibility and behavior changes. | Update for schema, configuration, dependency, or behavior changes. |
| `docs/decisions/README.md` | Current | ADR index and status rules. | Add and supersede ADRs through the index. |
| `docs/decisions/0001-schema-versioning.md` | Accepted | Schema compatibility, dual definitions, backup, and rollback rules. | Update only by superseding ADR. |
| `internal/app/prompts/*.md` | Executable | Main and plan agent behavior. | Change only with behavior tests. |
| `internal/agent/prompts/team/*.md` | Executable | Team role behavior. | Run team/subagent tests when changed. |
| `internal/config/prompts/subagents/*.md` | Executable | Default subagent prompts. | Keep config overrides aligned. |
| `internal/skills/bundled/*/SKILL.md` | Executable | Bundled Skill rules. | Run Skills catalog tests when changed. |

### Required maintainer documentation

| Priority | Target | Status | Minimum content |
|---|---|---|---|
| P0 | `docs/architecture.md` | Complete | Shared runtime, package boundaries, event flow, Agent/Store/Provider relationships, key call paths, extension points. |
| P0 | `docs/persistence.md` | Complete | SQLite paths, migration rules, SQLC sync, backups, compatibility, durable stores, recovery. |
| P0 | `docs/pull-requests.md` | Pending | Prerequisites, capability detection, lists/details, mutations, monitor state machine, polling/backoff, fingerprints, repair sessions, errors. |
| P0 | `docs/security.md` | Complete | Trust boundaries, approval modes, shell/filesystem/network, MCP, hooks, credentials, GitHub mutations, repair isolation. |
| P0 | `docs/testing.md` | Complete | Test levels and commands for Go, GPUI, desktop IPC, SQLite, PR, real GUI smoke, and live-test conditions. |
| P0 | `docs/release.md` | Pending | Versioning, dependency locks, `GOWORK=off`, complete tests, macOS packaging, schema upgrades, backup, rollback limits, release verification. |
| P1 | `docs/agent-runtime.md` | Complete | Venat v0.16 direct engines, durable Runtime/Backend, v1 bindings, application orchestration, exact recovery, usage, budgets. |
| P1 | `docs/provider-streaming.md` | Complete | Transports, Azem retry ownership, transient frames, durable effect settlement, `TextPhase`, usage, terminal events, errors. |
| P1 | `docs/desktop.md` | Complete | GPUI/daemon startup, IPC allowlist, event projection, native state, workspace browser, build, smoke test. |
| P1 | `docs/configuration.md` | Complete | Complete schema, defaults, environment, routes, roles, MCP, hooks, credentials, examples. |
| P1 | `docs/recovery.md` | Complete | Crash recovery, side-effect reconciliation, tool timeline, background work, Team resume, failures. |
| P1 | `docs/troubleshooting.md` | Pending | Login, streams, SQLite, `gh`, PR monitor, GPUI/IPC, Bun, Rust, logs. |
| P2 | `docs/accessibility.md` | Pending | Keyboard, focus, announcements, contrast, reduced motion, and native accessibility verification. |
| P2 | `CONTRIBUTING.md` | Pending | Environment, branches, commits, style, tests, documentation, PR checklist. |
| P2 | `CHANGELOG.md` | Current | User-visible changes, especially schema, configuration, dependencies, compatibility. |
| P2 | `docs/decisions/README.md` | Current | ADR numbering, status, supersession, index rules. |
| P2 | `docs/decisions/0001-schema-versioning.md` | Accepted | Future-schema rejection, dual definitions, backup, rollback principles. |
| P2 | `docs/decisions/0002-github-cli-boundary.md` | Pending | Why `gh`, argv boundary, authentication/permission, no stored GitHub token. |
| P2 | `docs/decisions/0003-shared-runtime.md` | Pending | Why TUI and GUI share runtime/store and Bridge exposes a closed operation set. |

## Documentation Update Rules

| Change | Required documentation |
|---|---|
| User feature | README, matching `docs/*.md`, optionally CHANGELOG |
| Configuration field/default | `docs/configuration.md`, README example, configuration tests |
| SQLite schema/persistence semantics | `docs/persistence.md`, schema ADR, CHANGELOG |
| Provider event/text phase | `docs/provider-streaming.md`, architecture flow |
| Agent/Venat scheduling | `docs/agent-runtime.md`, optionally ADR |
| Desktop operation/GPUI event | `docs/desktop.md`, security exposure |
| GitHub mutation/monitor | `docs/pull-requests.md`, `docs/security.md` |
| Build/test/release command | `docs/testing.md`, `docs/release.md`, README quick entry |
| Evidence-backed defect/incident | This issue table and, for user impact, a linked GitHub issue |
| Prompt text, tool order, or system/context injection | State the prefix-cache impact in the change description: whether the static prefix, message order, or per-turn injected content changes, and why cache hits survive (see PROVIDER-002) |

Every documented command must have been run. Paths, fields, states, and defaults
must come from current source, not memory.

## Verification Matrix

Run the narrowest relevant check first, then the complete check for the affected
surface before delivery.

| Change surface | Minimum verification |
|---|---|
| Go file | `gofmt -w <changed files>`; `go test <affected packages>` |
| Global Go/runtime/dependency | `GOWORK=off go test ./...` |
| Native GPUI/IPC | `make test-gpui`; IPC benchmarks; `make gpui`; launch `dist/Azem-GPUI.app/Contents/MacOS/Azem` and verify detach/reconnect |
| SQLite migration | `GOWORK=off go test ./internal/store/sqlite`; verify schema-27 upgrade to 28, current reopen, project/session retention, v1 blob hydration, unknown-attempt non-replay, and future rejection |
| Venat upgrade | Pinned upstream v0.16.1 contract packages; `GOWORK=off go mod tidy`; runtime package/race suites; `GOWORK=off go test ./...`; contracts/architecture checks; native desktop build and smoke |
| GitHub PR backend/UI | `go test ./internal/githubpr ./internal/desktop`; GPUI tests; verify list, detail, failure feedback, and changed mutation in an authenticated workspace |
| Provider streaming | Provider parser, app runtime, session persistence, and GPUI reducer/timeline tests |
| Prompt/bundled Skill | Matching app/agent/config/Skills tests and a real conversation path |
| Native security scan | `go test ./internal/securityscan ./internal/app ./internal/store/sqlite`; matching GPUI tests; complete Go/GPUI checks; real GUI Standard/Deep start-cancel and export path |
| Harbor eval adapter | `PYTHONPATH="$PWD" python3 -m unittest eval.harbor.timeout_test`; `go test ./cmd/azem-eval` |
| Architecture | `sentrux check .` and `session_end` with zero new cycles or violations |
| README/documentation | Validate paths, commands, links, and descriptions against current source |

A single passing test does not complete a feature. GUI changes require a real
GUI launch; migrations require opening a real database; external mutations
require both success and failure paths.

## Issue Record Format

When adding an issue row, include:

- A stable domain prefix and sequence, for example `DB-003`.
- One of the defined statuses.
- Observable user impact rather than a vague possibility.
- A test, error, source path, runtime log, or GitHub link as evidence.
- A confirmed root cause, or an explicit statement that it is unknown.
- The boundary to change and the regression coverage to preserve.
- The documentation index entry that must be updated.

Do not delete fixed incidents. Mark them **Fixed, guarded** and retain their
invariants and regression tests. Do not record transient build failures,
immediately corrected typos, or personal task progress.
