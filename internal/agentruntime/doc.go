// Package agentruntime owns Azem's durable run, governance, collaboration,
// and recovery facts. These records are application state; Venat owns only
// one direct Agent execution and its optional durable settlement protocol.
package agentruntime
